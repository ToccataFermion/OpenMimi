# OpenMimi 迭代开发进度

> 起始:2026-05-11。按 `docs/improvement_roadmap.md` 中"四、推荐执行顺序"推进。
> cron 每 6 分钟唤醒一次,读这个文件,继续下一项,完成后更新这里。

## 当前焦点

**Wave 5 #11 stage 2 (Replay CLI)**:在 stage 1 的 `replay.build_html(records)` 纯库基础上,加一层 CLI:`python -m openmimi.replay <session_id> [--out path.html] [--audit-dir ...] [--episodic-dir ...]`。**计划:** ① `replay.py` 加 `_cli()` 入口(argparse:session_id 必填,`--out` 默认 `replay_<sid>.html`,`--audit-dir`/`--episodic-dir` 路径覆写);② 新 helper `merge_records(episodic_records, audit_records)` —— 按 (step, ts) key 把 audit 的 `tool_input` / `image_path` / `duration_ms` 三个字段合并进 episodic 记录,episodic 没有的 step 也保留 audit 那侧(双侧 union);③ `build_html` 已经 happy-path 兼容这些额外字段(stage 1 走 "unknown keys → JSON payload" 路径),只要把 `image_path` 抽出来渲染成 `<img>` 即可,需要小改 `_render_step`;④ `__main__.py` 或 `if __name__ == "__main__"` block 调 `_cli()`;⑤ tests:argparse smoke + merge fixture(audit/episodic 各一条 → 合并后一条带 tool_input) + image_path 渲染为 img 标签 + missing audit 时 episodic 单独可渲染 + missing episodic 时 audit 单独可渲染。需要小心:image_path 是相对路径,HTML 里要保留相对路径让浏览器自动加载,不要强转 file:// 绝对 url(用户可能在 `data/audit/` 同目录开 HTML)。

> **Wave 5 #8 已完结(stages 1+2+3)** — `SubAgentRunner` + `SubAgentTool` + Orchestrator 接线齐备,主 LLM 现在可以 `spawn(task, allowed_tools=[...], max_turns=N)`。

> **Wave 5 #11 stage 1 已完成** — `replay.build_html(records)` 纯库渲染器,27 测试覆盖 escape / 缺字段 / generator / 错误标记 / meta 行;无第三方依赖。

> **Wave 4 #9 已结束(主线 stages 1+2+3 全部落地)** — stage 4/5 是可选升级,先按 roadmap 顺序推 Wave 5。

## Wave 1 — 清债 + 基础(本周)

- [x] #1 删除 legacy `browser.py`,从 `tools/__init__.py` 摘掉 `BrowserTool` 导出
- [x] #2 修复 / 移除 skills 加载(`skills.py` 指向已删除目录)
- [x] #3 错误码扩容 + 结构化恢复提示(20 个 + `next_step_hint` + `make_error_result` helper)
- [x] #6 Daemon prewarm(REPL 启动时即触发 `_ensure_daemon`)

## Wave 2 — 工程解耦(下周)

- [x] #4-lite `agent_browser.py` dispatcher 化(拆 `actions/` 子目录,主类只做路由 + daemon 管理)
  - [x] step 1 — `actions/` 包 + registry + `navigation.py`(navigate/back/forward/reload)
  - [x] step 2 — interaction 族(click/right_click/double_click/check/uncheck/type/fill/react_fill/press/key_combo/hover)
  - [x] step 3 — scroll 族(scroll/human_scroll/scroll_until/scroll_into_view)
  - [x] step 4 — extract / page-state 族(snapshot/extract/page_source/get_url/get_title/get_attribute/set_attribute/get_property/get_box/is_visible/visual_locate)
  - [x] step 5 — wait 族(wait/wait_for/wait_for_disappear/wait_for_navigation/wait_for_network_idle)
  - [x] step 6 — tab/session 族(tab_*/save_session/load_session/clipboard/clear_cache)
  - [x] step 7 — network/cdp/storage/pdf/console/screenshot 族
  - [x] step 8 — 剩余杂项(eval/batch/drag/mouse/focus/select/upload/download/emulate_device/set_*)
  - [x] step 9 — 主类只剩 `__init__` / `__call__` / `_dispatch` / daemon 管理(`_TOOL_DESCRIPTION` + `to_params` schema 抽出到 `agent_browser_schema.py`)
- [x] #12 结构化 ToolResult(新增可选 `structured: dict` 字段)

## Wave 3 — 智能跃迁(2-3 周)

- [x] #7 Planner / Executor / Verifier 三角
  - [x] 阶段 1 — `Plan`/`PlanStep`/`Verifier` 数据结构 + Protocol + `NullVerifier`(默认关闭,`AppConfig.enable_planning=False`)
  - [x] 阶段 2 — 在 `loop.py` 里接入 Verifier 做 turn-end 自评(返回 done / continue / replan)
  - [x] 阶段 3a — `LLMVerifier`(用 LLMClient 给 success_criteria 打分,失败安全降级"continue")
  - [x] 阶段 3b — `LLMPlanner`(任务分解 → `[{step, success_criteria, allowed_tools?, budget?}]`,失败时单步 plan)
  - [x] 阶段 3c — Orchestrator 接线 + `enable_planning` gate + system prompt 注入
- [x] #5 Token budget + 智能 LLM 压缩(替代 400 字符截断)
  - [x] 阶段 1 — `AppConfig` 加 `max_context_tokens: int = 80000` 与 `compression_strategy: Literal["truncate","summarize"] = "truncate"`(默认保留旧行为)
  - [x] 阶段 2 — `compress_tool_result(text, llm, target_chars=500)` helper + `estimate_tokens(text)`(纯库函数,失败时降级截断)
  - [x] 阶段 3 — `loop.py` 接 strategy 与 `max_context_tokens` 软上限
  - [x] 阶段 4 — `Orchestrator` 在 `strategy="summarize"` 时把 LLMClient 透传给 sampling_loop

## Wave 4 — 跨 session 记忆(3-4 周)

- [x] #9 Memory v2(grep 版):episodic / semantic / procedural 三层 + `memory_grep/read/write/list` 工具(主线 stages 1-3 完成,stage 4/5 是可选升级,见 ## 已完成)
  - [x] 阶段 1 — `EpisodicStore` 数据层(`src/openmimi/memory/episodic.py`):JSONL 落盘 + append/read/list,session_id 路径安全)
  - [x] 阶段 2 — 把 `EpisodicStore` 接进 `loop.py`,每个 tool 调用后追加一条 step record
  - [x] 阶段 3 — `memory_grep / memory_read / memory_write / memory_list` 4 个 `ToolBase` 子类 + Orchestrator 注册
  - [ ] 阶段 4(可选)— 把现有 `SiteMemoryStore` 的 JSON 升级成 markdown
  - [ ] 阶段 5(可选)— procedural skills 目录

## Wave 5 — 隔离 + 调试

- [x] #8 Sub-agent 隔离(`spawn(task, tools_subset, max_turns) → result`)
  - [x] 阶段 1+2 — `SubAgentRequest`/`SubAgentResult` 数据类 + `filter_tools` + `default_sub_session_id` + `SubAgentRunner.run` 实现 + 36 个测试(见 ## 已完成)
  - [x] 阶段 3 — 包装成 `sub_agent` `ToolBase` + Orchestrator 注册 + 跨 turn 的 session_id 提供器
- [ ] #11 轻量 Replay 脚本(`python -m openmimi.replay <session_id>` → 单页 HTML)
  - [x] 阶段 1 — `replay.build_html(records)` 纯库渲染器(stdlib-only,XSS-safe,27 测试,见 ## 已完成)
  - [ ] 阶段 2 — `python -m openmimi.replay <sid>` CLI + audit/episodic merge + 可选 `--out`

## 工作守则(给未来唤醒的我)

1. 每次唤醒先 `git status` + 读这个文件,确认从哪里继续。
2. 一次只做一小步(目标:每次唤醒能产出 1 个 commit,或在长项目中推进一个明确小阶段)。
3. 完成后必须:
   - 跑现有测试 `pytest tests/` 确认没破坏
   - 创建 git commit(信息清晰、作用域窄)
   - 在本文件上方标 `[x]`,把"当前焦点"指到下一项
4. 不能自主决策的问题(API/架构方向不明、需要外部账号等)→ 写入 `docs/blockers.md`,跳过该项继续下一个。
5. 不要扩张范围:遇到看似相关但不在已选项内的事项,记到本文件末尾的"延期项"下,不要现场改。
6. 失败/破坏性操作前(`rm -rf` / `git reset --hard` / 强推)→ 一律先停手记录到 blockers,等用户回来。

## 已完成

- 2026-05-11 · **Wave 1 #1** — 删除 `browser.py` (1108 行)、`browser_schema.py` (344 行)、相关测试与脚本;`tools/__init__.py` 收敛导出。`pytest`: 62 passed / 2 pre-existing failures(与本改动无关,见 Blockers)。
- 2026-05-11 · **Wave 1 #2** — 删除 `src/openmimi/skills.py`(指向已删除目录的死代码);**顺手修复**:`Orchestrator._build_system_prompt` 之前完全没注入 site memory(写但不读),现在通过 `memory.format_for_prompt(domain)` 真正接到 system prompt 上。`pytest`: 仍 62 passed / 2 同样的 pre-existing failures。
- 2026-05-11 · **Wave 1 #3** — `tools/errors.py` 从 5 个 ErrorCode 扩到 20 个,覆盖 element / network / auth / page-state / execution / system 六类;每个 code 配一条 `next_step_hint` 结构化恢复提示;新增 `make_error_result(code, message, ...)` helper,把提示同时写进 `output`(LLM 可见)和 `details`(审计);新增 `tests/unit/test_errors.py`(7 例,覆盖 hint 完整性 / StrEnum 兼容 / extra_details 合并)。`pytest`: 69 passed / 2 同样的 pre-existing failures。**注**:Wave 1 #4 之前不大改 `agent_browser.py`,所以暂未把现有错误路径迁到新 helper(留待 dispatcher 化时一起做)。
- 2026-05-11 · **Wave 1 #6** — Daemon prewarm 显式化。背景:`AgentBrowserTool.__init__` 已经在后台线程跑 `_start_warmup`,但用户感知不到首条任务为什么慢。本次改动:① `AgentBrowserTool.is_warming_up()` 公开接口;② `Orchestrator` 持有 `browser_engine` 引用,新增 `prewarm_browser() -> bool`(失败安全);③ `cli.chat()` 与 `cli.chat_main()` 在 welcome 之后调用 `_announce_prewarm(orch)`,有 warmup 在飞时打一行 `browser : warming up in background (first task may be slow)`。新增 `tests/unit/test_prewarm.py`(7 例,覆盖无 engine / 飞 / 完成 / 异常 / 三种 announce 路径)。`pytest`: 76 passed / 2 同样的 pre-existing failures。**注**:没有真正"挪"启动时间(它本来就在后台跑),而是把它**可见化** — 这是用最小改动达成 roadmap 意图的方式,等 Wave 1 #4 dispatcher 化之后如果还需要可以再做 sync await。
- 2026-05-11 · **Wave 2 #4-lite step 1** — `agent_browser.py`(3766 行)dispatcher 化第 1 步:① 新建 `tools/actions/` 包 + 注册表(`@register(name)` 装饰器、`get(name)`、`registered_actions()`);② 新建 `actions/navigation.py`,把 `navigate/back/forward/reload` 4 个 handler 从 `_do_*` bound method 迁出为接受 `engine` 参数的自由函数;③ `_dispatch` 在查询内置 dict 之前先咨询 registry,registry 命中即返回;④ 删除 `agent_browser.py` 里这 4 个 method 与对应的 dispatch 项(-40 行)。增量、向后兼容(主类外观完全不变)、不需要再回炉 — 后续 step 复用同一脚手架。新增 `tests/unit/test_actions_registry.py`(6 例,覆盖注册表查询 / 4 个 handler 与 fake engine 的协作)。`pytest`: 82 passed / 2 同样的 pre-existing failures。`agent_browser.py` 现 3726 行,目标是 < 1000 行。
- 2026-05-11 · **Wave 2 #4-lite step 2** — interaction 族 11 个 handler 外迁:① 新建 `actions/_keys.py`(verbatim 搬 `_cdp_key_code` + `_KEY_MAP`,留作 `actions/` 包内部 helper);② 新建 `actions/interaction.py`,迁 `click/right_click/double_click/check/uncheck/type/fill/react_fill/press/key_combo/hover` 11 个 handler + `_click_with_mouse` 私有 helper(自由函数版),全部用 `@register("name")` 装饰;③ `actions/__init__.py` 加 `from . import interaction` side-effect 注册;④ `agent_browser.py` 删 11 个 `_do_*` 方法 + `_click_with_mouse` 方法 + 4 份重复的 `_cdp_key_code` 顶层函数(原本只有 1 份,Edit 操作中放大了一次,清理时一并 verbatim-replace_all 删干净)+ dispatch 表里 11 个对应项。**净变化**: -407 行,`agent_browser.py` 从 3726 → 3319 行。新增 5 个新测试(交互族注册成员检查 + click/type/press/right_click 4 个 handler 行为冒烟,假 engine 走 `get box → mouse move/down/up` 三步序列)。`pytest`: 88 passed / 2 同样的 pre-existing failures。
- 2026-05-11 · **Wave 2 #4-lite step 3** — scroll 族 4 个 handler 外迁:① 新建 `actions/scroll.py`,迁 `scroll/human_scroll/scroll_until/scroll_into_view`;`scroll_until` 内部依赖 `engine._parse_data` / `engine._parse_snapshot`,`scroll_into_view` 走 `eval` 注入 JS,`human_scroll` 用 `random` + `asyncio.sleep` 模拟人;② `actions/__init__.py` 加 `from . import scroll` side-effect 注册;③ `agent_browser.py` 删 4 个 `_do_*` 方法 + dispatch 表里 4 个对应项。本次 Edit 中途因为 new_string 写错把方法块复制了一份(从而临时变成 3464 行),靠后续 `replace_all=true` 把 4-method 块 + 后随的 `_do_page_source` 行作为唯一 anchor 一次性删 1 份(只删了 1 份,因为另一份的 anchor 行不一致),再用 `replace_all=false` 精确删剩余 1 份,最终回到 3165 行(净 -154)。新增 5 个测试:scroll 族注册成员、`scroll` 子命令转发、`scroll_until` 首探就找到立即返回、`scroll_until` 缺参数报错、`scroll_into_view` 走 `eval`。`pytest`: 93 passed / 2 同样的 pre-existing failures。`agent_browser.py` 现 3165 行。
- 2026-05-11 · **Wave 2 #4-lite step 4** — extract / page-state 族 11 个 handler 外迁:① 新建 `actions/extract.py`(~530 行),迁 `snapshot/page_source/get_url/get_title/get_attribute/set_attribute/get_property/extract/get_box/is_visible/visual_locate`;`snapshot` 内嵌 CAPTCHA 检测路径,失败时返回带 `error_code: ErrorCode.CAPTCHA_DETECTED` 的非错误 ToolResult,提示 LLM 看截图解题;`get_attribute` / `set_attribute` / `get_property` 共享 ref → CSS-selector / target_text → treeWalker 的元素定位 JS 模板;`extract` 按 instruction 分支(get text / headings / links / forms / tables / metadata / images / 兜底)各自构造 JS,输出截到 4000 字符;`visual_locate` 用 OpenCV 模板匹配,临时把 `engine._screenshot_scale` 设 1.0 拿全分辨率截图后在 `finally` 还原,可选 click 走 mouse move/down/up;② `actions/__init__.py` 加 `from . import extract` side-effect 注册;③ `agent_browser.py` 删 11 个 `_do_*` 方法 + dispatch 表里 11 个对应项,3 批 Edit(snapshot+extract / get_box+is_visible+visual_locate / page_source+get_url+get_title+get_attribute+set_attribute+get_property)。**净变化**:-431 行,`agent_browser.py` 从 3165 → 2734 行,registry 现含 30 个 action(4 nav + 11 interaction + 4 scroll + 11 extract)。新增 7 个测试:extract 族注册成员检查 + `get_url`/`get_title`/`get_box` 子命令路由 + `get_box` 缺 selector 报错 + `extract` "get text" 截 4000 字符 + `get_attribute` 缺 attribute_name 报错 + `is_visible` 走 eval 含 getBoundingClientRect。`pytest`: 101 passed / 2 同样的 pre-existing failures。
- 2026-05-11 · **Wave 2 #4-lite step 5** — wait 族 5 个 handler 外迁:① 新建 `actions/wait.py`(~258 行),迁 `wait/wait_for/wait_for_disappear/wait_for_navigation/wait_for_network_idle`;`wait` 单纯转发 daemon 的 `wait` 子命令,其余 4 个都在 Python 端轮询(`time.monotonic` + `asyncio.sleep`)以避免占用 sidecar;`wait_for_navigation` 起步先抓一次当前 URL,然后轮询 `eval window.location.href` 直到变化(可选 `expected_url` 子串过滤);`wait_for_network_idle` 一次性向页面注入 `window.__openmimi_network_idle_*` fetch/XHR 拦截器(再注入幂等),然后轮询 in-flight 计数与 last-active 时间戳;② `actions/__init__.py` 加 `from . import wait` side-effect 注册;③ `agent_browser.py` 删 5 个 `_do_wait*` 方法(3 批 Edit:`_do_wait` 单独删 + 中途多删了 `async def _do_eval` 头部,立即用一次反向 Edit 修复 / `_do_wait_for`+`_do_wait_for_disappear` 合块删 / `_do_wait_for_navigation`+`_do_wait_for_network_idle` 合块删)+ dispatch 表 3 批共 5 项删除。**净变化**:-219 行,`agent_browser.py` 从 2734 → 2515 行,registry 现含 35 个 action。新增 7 个测试:wait 族注册成员检查 + `wait` 子命令转发 + `wait_for` 首探到立即返回 + `wait_for` 缺 target 报错 + `wait_for_disappear` 首探无 box 立即返回 + `wait_for_navigation` URL 变化检测 + `wait_for_network_idle` 注入 + idle 立即返回。`pytest`: 108 passed / 2 同样的 pre-existing failures。
- 2026-05-11 · **Wave 2 #4-lite step 6** — tab/session 族 8 个 handler 外迁:① 新建 `actions/tab_session.py`(305 行),迁 `clipboard/tab_list/tab_switch/tab_new/tab_close/clear_cache/save_session/load_session`;`tab_*` 4 个 handler 都先 `engine._refresh_tabs()` 再读 `engine._tabs` / 写 `engine._active_tab_index` 保持 sidecar 与 Python 镜像一致;`save_session` 优先用 CDP `Network.getAllCookies`(可拿 HTTP-only),失败回落 `document.cookie`;`load_session` 同时支持 CDP cookie 数组(`{name,value,domain,path}`)与 legacy 分号串两种存盘格式;② `actions/__init__.py` 加 `from . import tab_session` side-effect 注册;③ `agent_browser.py` 删 8 个 `_do_*` 方法(3 批:clipboard+tab_list+tab_switch+tab_new+tab_close 一块 / clear_cache 一块 / save_session+load_session 一块)+ dispatch 表 3 批共 8 项删除。**净变化**:-258 行,`agent_browser.py` 从 2515 → 2257 行,registry 现含 43 个 action。新增 8 个测试:tab_session 族注册成员检查 + `clipboard read/write` 转发 + `tab_list` 列表 + `tab_new` 子命令 + `save_session`/`load_session` 缺 file_path 报错 + `clear_cache` 走 eval 含 `localStorage.clear()`。`pytest`: 116 passed / 2 同样的 pre-existing failures。
- 2026-05-11 · **Wave 2 #4-lite step 7** — network/cdp/storage/pdf/console/screenshot 族 7 个 handler + 1 helper 外迁:① 新建 `actions/network_cdp.py`(708 行),迁 `cdp/screenshot/network_log/network_modify/storage/pdf/console`;`cdp` 通过 `window.__openmimi_cdp_send(method, params)` JS bridge 转发任意 CDP 方法,作为逃生口;`screenshot` 仍走 `engine._take_screenshot(path_override, annotate)`,但本族独立 import 了 `screenshots_disabled`(`agent_browser.py` 仍保留 import 因为 `_take_screenshot_raw` 也要用);`network_log` 注入幂等 `window.__openmimi_network_log` 拦截 fetch / XHR(再注入幂等),再读最多 20 条捕获;`network_modify` 5 个子动作(user_agent / inject_headers / block_urls / mock_response / clear),user_agent 优先走 CDP `Network.setUserAgentOverride` 失败回落 `Object.defineProperty`;`storage` 三类(localStorage / sessionStorage / cookies),cookies 优先走 CDP `Network.getAllCookies` / `Network.setCookie`(经 `_try_cdp_then_fallback` 自由函数 helper 串起来),失败回落 `document.cookie`;`pdf` 走 CDP `Page.printToPDF`,失败回落 `window.print()` 提示;`console` 注入 `window.__openmimi_console_logs`(max 200)shim,读最近 30 条按 level 过滤;② `_try_cdp_then_fallback` 从 method 改为自由函数(首参 engine),storage handler 内部更新调用;③ `actions/__init__.py` 加 `from . import network_cdp` side-effect 注册;④ `agent_browser.py` 删 7 个 `_do_*` 方法 + 1 个 `_try_cdp_then_fallback` helper(4 批 Edit:cdp+screenshot 一块 / network_log 单独 / network_modify 单独 / storage+helper+pdf+console 一块)+ dispatch 表 1 批共 7 项删除。**净变化**:-669 行,`agent_browser.py` 从 2257 → 1588 行,registry 现含 50 个 action。新增 14 个测试:network_cdp 族注册成员检查 + `cdp` value 拆封 + 缺 cdp_method 报错 + `screenshot` 启用/禁用两路 + `network_log` 安装+读 + `network_modify` user_agent / inject_headers 缺 headers + `storage` localStorage set / 缺 key / cookies CDP-first + `pdf` 缺 file_path / 成功 + `console` 安装+读。`pytest`: 130 passed / 2 同样的 pre-existing failures。`agent_browser.py` 现 1588 行,距目标 < 1000 还差 ~588 行,1-2 步可达。
- 2026-05-11 · **Wave 2 #4-lite step 8** — 杂项 13 个 handler 外迁(最后一族!):① 新建 `actions/misc.py`(~470 行),迁 `select/upload/download/eval/batch/drag/mouse/focus/set_viewport/emulate_device/set_timezone/set_locale/set_geolocation`;`select/upload/download` 是 sidecar passthrough(`engine._exec("select", ...)` 等);`eval` 用 `engine._parse_json` 拆封 success/data/result 三层结构,空结果给提示;`batch` 把 steps 数组直接拼到 `engine._exec("batch", "--bail", "--json", ...)`;`drag` 支持 ref+to_ref 或 target_text+to_target_text;`mouse` 4 个子动作(move/down/up/wheel)直接转 sidecar;`focus` Win32 EnumWindows + 评分(title 3 / domain 2 / url 2 / chromium 1),`urlparse` 改为 module-level import;`set_viewport` 走 `eval` 注入 `window.resizeTo()`;`emulate_device` 把 `_DEVICE_PRESETS` 提为模块常量,先试 CDP `Emulation.setDeviceMetricsOverride`(+ `setUserAgentOverride` + 可选 `setTouchEmulationEnabled`),失败回落 JS-only(`window.resizeTo` + `Object.defineProperty navigator.userAgent`);`set_timezone/set_locale/set_geolocation` 都是单条 CDP `Emulation.set*Override` eval 调用,`set_geolocation` 在缺 lat/lon 时调 `clearGeolocationOverride`;② `actions/__init__.py` 加 `from . import misc` side-effect 注册;③ `agent_browser.py` 删 13 个 `_do_*` 方法 + dispatch 表彻底清空(`_dispatch` 现在只保留 registry 查询,miss 即返回 `Unknown action: ...`,3 批 Edit:select+upload+download / emulate_device+set_timezone+set_locale+set_geolocation / eval+batch+drag+mouse+focus+set_viewport)。**净变化**:-414 行,`agent_browser.py` 从 1588 → 1174 行,registry 现含 63 个 action(全部 action handler 已外迁,只剩特殊路径 `_do_close`,因为它在 `__call__` 中走前置短路而非 `_dispatch`)。新增 16 个测试:misc 族注册成员检查 + select/upload 缺参数 + download 缺 selector + eval 空 js / 成功序列化 + batch 缺 steps + drag 缺配对目标 + mouse move 转发 + set_viewport 缺尺寸 / 成功 eval + emulate_device CDP 路径 + set_timezone 含 timezoneId + set_locale 含 locale + set_geolocation 清除 / 设置。`pytest`: 146 passed / 2 同样的 pre-existing failures。`agent_browser.py` 现 1174 行,距目标 < 1000 还差 ~174 行,step 9 (主类瘦身) 可达。
- 2026-05-11 · **Wave 2 #4-lite step 9 (#4-lite 完结)** — `_TOOL_DESCRIPTION` 长字符串(76 行)+ `to_params()` JSON Schema(330 行,含 64 项 action enum + 59 个 property 描述)抽出到新模块 `tools/agent_browser_schema.py`(431 行,纯数据,无 import 依赖 instance state);`agent_browser.py` 改为 `from .agent_browser_schema import TOOL_DESCRIPTION as _TOOL_DESCRIPTION, build_input_schema as _build_input_schema`,`to_params()` 缩为 6 行 thin wrapper(只组装 `{name, description, input_schema}` 三键 dict);`build_input_schema()` 每次 fresh 返回新 dict 避免共享可变状态。**净变化**:-401 行,`agent_browser.py` 从 1174 → 773 行(从起步 3766 行降了 -2993 行 / -79.5%,远低于 < 1000 行目标);schema 模块化后,如果未来需要加新 action,只需改一个地方而不是两个。**未抽出**:daemon 管理方法(`_start_browser` / `_exec` / `_take_screenshot` / `_parse_*` / `_inject_stealth` / `_refresh_tabs` / `_take_screenshot_raw` / `_browser_env` / `_start_warmup` / `is_warming_up` / `_detect_captcha`)与 `_do_close` 都依赖 instance state(`self._daemon_proc`、`self._tabs`、`self._screenshot_scale` 等),抽出会引入大量 engine 参数注入,得不偿失,留在主类。**未新增测试**:schema 抽出是纯文本搬迁,运行时行为不变,现有 146 个测试已经覆盖 `to_params()` 间接路径(每次 `Tool.to_params()` 调用都会触发 `_build_input_schema()`)。`pytest`: 146 passed / 2 同样的 pre-existing failures。**`#4-lite` 已完结**:从 3766 行 god class 拆成 1174 行(step 8) → 773 行(step 9)主类 + 8 个 actions/ 子模块(navigation/interaction/scroll/extract/wait/tab_session/network_cdp/misc)+ 1 个 agent_browser_schema.py,共 63 个 action handler 全部走 `@register` 注册表分发,主类只剩 daemon 管理 + 路由 + 一个特殊 close 短路。
- 2026-05-11 · **Wave 2 #12** — 结构化 `ToolResult.structured` 字段(可选 `dict[str, Any]`,默认 `None`)。设计要点:`output: str` 仍是 LLM 可读文本,`details: dict` 仍是元数据(error_code / next_step_hint),新增的 `structured` 是**数据载荷本身**(即 handler 内部已经构造好的 dict / list,免得上层再 re-parse `output` 字符串)。在 `tools/result.py` `@dataclass ToolResult` 加字段并写明语义注释。挑 4 个高价值 handler 做示范:① `extract`(get text / 6 个 instruction 分支)— `structured={"instruction": ..., "data": result_value}`;② `get_box` — `structured={"box": box, "selector": selector}`(与现有 `details` 同步);③ `is_visible` — `structured=result_value`(复用 result_value 的 visible/tag/rect dict);④ `network_log` — `structured={"requests": requests}`(20 条最近抓到的请求列表)。loop.py 不动 — `structured` 是给 Wave 3 #7 的 Planner 与 Wave 5 #8 的 Sub-agent 用的,LLM 端继续读 `output`。新增 6 个测试:`structured` 默认 None + 上述 4 个 handler 各自的 structured 内容断言 + extract links 列表场景。`pytest`: 152 passed / 2 同样的 pre-existing failures(从 146 → 152,+ 6 个新测试)。**Wave 2 完结**(#4-lite + #12 都已 [x])。
- 2026-05-11 · **Wave 3 #7 阶段 1** — Planner/Verifier 脚手架。新建 `src/openmimi/planning.py`(132 行,纯数据 + Protocol):`@dataclass PlanStep(step, success_criteria, allowed_tools=None, budget=None, done=False)` + `to_dict/from_dict`;`@dataclass Plan(steps, current_step=0)` + `current()`/`advance()`/`is_complete()`/`to_dict/from_dict`;`@runtime_checkable Verifier` Protocol(单方法 `async verify(plan, recent_messages) -> Literal["done","continue","replan"]`);`NullVerifier`(永远返回 `"continue"`)作为安全默认。`AppConfig` 加 `enable_planning: bool = False` 开关 — 阶段 1 完全无运行时影响,只是为阶段 2 准备好"骨架接得上"。**没动 `loop.py`**,这是阶段 2 的事。新增 `tests/unit/test_planning.py`(12 例:dataclass 默认值 + roundtrip + advance/current/is_complete 三态 + 空 Plan / 越界 advance 安全 + NullVerifier 三种入参路径 + Protocol isinstance 检查 + AppConfig 默认值)。`pytest`: 164 passed / 2 同样的 pre-existing failures(从 152 → 164,+12 个新测试)。下一步:阶段 2 把 Verifier 接进 `loop.py` 做 turn-end 自评。
- 2026-05-11 · **Wave 3 #7 阶段 2** — Verifier 接进 `loop.py`。`sampling_loop` 加 `verifier: Verifier | None = None, plan: Plan | None = None` 两个 keyword-only 参数(都默认 None,所有现有 caller 行为 100% 不变)。在每 turn 末尾(`messages.append({"role": "user", "content": tool_result_blocks})` + `_trim_old_images` + `_compress_old_tool_results` 之后),如果 `verifier and plan and not plan.is_complete()`,就 `await verifier.verify(plan, messages)`;outcome:`done` → `return messages` 提前结束;`replan` → 打 `[planner] verifier requested replan (stage 3 hook)` stderr 标记继续(阶段 3 在这里挂 Planner);`continue` 或任何意外值 → 无操作。Verifier 抛异常被 try/except 吞掉(降级 `continue`)— 一个坏 verifier 不会拖垮整个会话。`Orchestrator` 不动,因为新参数都默认 None。新增 7 个 `test_loop.py` 测试 + 一个 `_RecordingVerifier` 假实现:no verifier 跳过 / 有 verifier 但 plan=None 跳过 / NullVerifier 不影响循环 / done outcome 早退(只跑 1 turn 而不是 3 turn) / replan 不影响 / verifier 异常被吞 / 完成 plan 跳过。`pytest`: 171 passed / 2 同样的 pre-existing failures(从 164 → 171,+7 个新测试)。下一步:阶段 3 接入 Planner LLM(任务分解 → 子目标列表)+ `LLMVerifier`(用便宜模型给 success_criteria 打分),把 #7 整项收尾。
- 2026-05-11 · **Wave 3 #7 阶段 3a** — `LLMVerifier` 落地。`src/openmimi/planning.py` 在 `NullVerifier` 之后新增:① `_LLM_VERIFIER_SYSTEM` 常量(strict-JSON 系统提示,要求 `{"outcome":"done|continue|replan","reason":"..."}` 无 prose/markdown/fences);② `_OUTCOMES` 元组用于枚举校验;③ `_extract_response_text(response)` 从 Anthropic 响应里抽文本块(兼容 `content: str | list[block]`);④ `_parse_outcome(text)` — 先直接 `json.loads(text.strip())`,失败回退 `re.search(r"\{.*\}", text, re.DOTALL)` 抓首个 `{...}`,然后校验 `outcome ∈ _OUTCOMES`,无效返回 None;⑤ `_summarize_messages(messages, max_chars_per_block=400)` 把 Anthropic 消息渲染成 `[role/type] payload` 一行一条(text / tool_use / tool_result(含 ERROR 标记) / image 各自分支),每块截 400 字符给廉价模型友好的小 prompt;⑥ `LLMVerifier(llm, *, max_tail_messages=6, max_tokens=200, system_prompt=None)` — `verify(plan, msgs)` 中 `plan.current() is None` 直接返回 `"done"`(免 LLM 调用),否则 tail-clip + 构造 user prompt(含 step / success_criteria / 活动摘要 / 末尾"Reply with ONLY"指令)调 `llm.create(system, messages, tools=[], max_tokens)`,**任何异常**(网络 / API / JSON / 枚举无效)都降级到 `"continue"` — 一个坏 verifier 永远不能拖垮一个能跑的会话。`__all__` 加上 `LLMVerifier`。新增 20 个测试:`_parse_outcome` 5 个 valid(普通 / 紧凑 / replan / 前置 prose / fenced JSON)+ 6 个 invalid(空 / 无 json / outcome 值非法 / 缺 outcome key / 烂 JSON / 裸字符串)、`_summarize_messages` 2 个(综合 + ERROR 标记)、`LLMVerifier` 7 个(空 plan 不调 LLM / done passthrough / continue+replan 连环 / 烂 JSON 降级 continue / 异常降级 continue / tail-clip 只保留末 N 条 / Protocol isinstance)。`pytest`: **191 passed** / 2 同样的 pre-existing failures(从 171 → 191,+20 个新测试)。Commit: `30f417f feat(planning): add LLMVerifier (#7 stage 3a)`。**注**:`LLMVerifier` 持有任意 `LLMClient`(调用方挑模型 — Orchestrator 阶段 3c 会接一个 Haiku-class 廉价模型),所以测试不需要造任何真实 LLM provider;`_ScriptedLLM` stub 单类够用。下一步:阶段 3b 实现 `LLMPlanner`,同样的"失败永不阻塞"策略 + `_ScriptedLLM` 复用。
- 2026-05-11 · **Wave 3 #7 阶段 3b** — `LLMPlanner` 落地。`src/openmimi/planning.py` 在 `LLMVerifier` 之后新增:① `_LLM_PLANNER_SYSTEM` 常量(strict-JSON 系统提示,要求 top-level array,每项 `{step, success_criteria, allowed_tools?, budget?}`,1-5 步 / 复杂 ~8 步);② `_parse_plan_steps(text)` — 先直接 `json.loads`,失败回退 `re.search(r"\[.*\]", text, re.DOTALL)` 抓首个 `[...]`,逐项校验 — `step` 与 `success_criteria` 必须是非空字符串(strip 后),`allowed_tools` 若是列表则只留非空字符串项(空列表归一为 None),`budget` 必须是正 int(显式排除 `bool` — Python 里 `True is int`),malformed 项跳过;若一项都没活下来返回 None;③ `_single_step_plan(task)` — 兜底 Plan,把 task 包成单步 `step=task, success_criteria=f"task accomplished: {task}"`,空 task 换成"complete the user's request";④ `LLMPlanner(llm, *, max_tokens=1024, system_prompt=None)` — `plan_task(task, system_context="")` 中空 task 直接返回 `_single_step_plan` 不调 LLM;否则构造 user prompt(含可选 `Environment context: ...` 段 + `User task: ...` + 末尾"Reply with ONLY"示例)调 `llm.create(...)`,**任何异常 / 烂 reply / 空 array** 都降级到 `_single_step_plan(clean_task)` — `plan_task` 永远返回可用 Plan,never None never raises。`__all__` 加上 `LLMPlanner`。新增 24 个测试:`_parse_plan_steps` 7 个 valid(最小 / 可选字段 / strip / 噪声前后 / 跳过 malformed / 过滤非字符串 tools / 过滤无效 budget)+ 7 个 invalid(空 / 无 json / 空 array / 全 malformed / dict 非 array / 烂 json / 裸字符串)、`_single_step_plan` 2 个(普通 / 空 task)、`LLMPlanner` 8 个(happy path / task+context 进 prompt / 空 context 不出现 "Environment context" / 空 task 不调 LLM / 烂 reply 降级 / 空 array 降级 / 异常降级 / 部分 malformed 项被丢)。`pytest`: **215 passed** / 2 同样的 pre-existing failures(从 191 → 215,+24 个新测试)。Commit: `d4cdf88 feat(planning): add LLMPlanner (#7 stage 3b)`。下一步:阶段 3c 把 LLMPlanner + LLMVerifier 接到 `Orchestrator`,在 `enable_planning=True` 时先 `await planner.plan_task(...)` → 注入 system prompt + 把 plan/verifier 透传给 `sampling_loop`。
- 2026-05-11 · **Wave 3 #7 阶段 3c (#7 整项完结)** — Orchestrator 接线 + `enable_planning` gate。`src/openmimi/orchestrator.py` 改动:① 顶部 import `from .planning import LLMPlanner, LLMVerifier, NullVerifier, Plan, Verifier`;② `_build_system_prompt(domain, memory, plan=None)` 多接一个可选 `plan: Plan | None`,plan 非空时把 `_format_plan_summary(plan)` 追加到 extras(在 site memory 之后);③ 新增 `_format_plan_summary(plan)` 把 plan 渲染成 `"Planned approach (verifier will grade each step):\n  1. <step> [success: <criteria>]\n  ..."`(空 plan 返回空串);④ `Orchestrator.__init__` 加两个可选 kw `planner: LLMPlanner | None = None, verifier: Verifier | None = None`,存为 `self.planner` / `self.verifier`(未传都 None);⑤ 新增 `_maybe_plan_task(task, domain) -> Plan | None`:`enable_planning=False` 或 `planner is None` 时返回 None,否则 `await planner.plan_task(task, _planner_context(domain))` 抓 Plan;`_planner_context(domain)` 把 site memory 串作为 planner 的 system_context;任何异常吞掉返回 None(LLMPlanner 自己已经兜底,但防御性双保险);⑥ 新增 `_effective_verifier(plan)`:无 plan 或 planning 关时返回 None(loop 那边会跳过 verifier 调用);有 plan 时优先用注入的 verifier,否则 `NullVerifier()` — 保持 loop 接口一致;⑦ `run_task` / `run_chat_turn` 都改成 `plan = await self._maybe_plan_task(input, domain)` → `system = _build_system_prompt(domain, memory, plan)` → `verifier = self._effective_verifier(plan)` → `sampling_loop(..., plan=plan, verifier=verifier)`;⑧ `from_env` 在 `cfg.enable_planning=True` 时构造 `planner = LLMPlanner(llm)` 与 `verifier = LLMVerifier(llm)`(复用主 LLMClient,模型一致 — `model_override` 的细分留给以后,符合 stage 3c 的工程口径决策),`False` 时保持 None。新增 11 个 `test_orchestrator.py` 测试 + `_ScriptedPlanner` 假实现 + `_capture_sampling_loop` monkey-patch helper:`_format_plan_summary` 正常 / 空 plan、`_build_system_prompt` 含 plan / 空 plan 不变、`run_task` planning off 跳过 planner / planning on 调 planner / null verifier 兜底 / 异常吞掉、`run_chat_turn` 每轮调 planner、`from_env` 开关两路。`pytest`: **226 passed** / 2 同样的 pre-existing failures(从 215 → 226,+11 个新测试)。Commit: `f2f3f57 feat(orchestrator): wire LLMPlanner + Verifier (#7 stage 3c)`。**`#7` Planner / Executor / Verifier 三角整项完结**(stage 1 数据结构 + stage 2 loop 接 verifier + stage 3a LLMVerifier + stage 3b LLMPlanner + stage 3c Orchestrator 接线)。**注**:`AppConfig.enable_planning` 仍默认 False,需要用户显式打开才会启用;Wave 3 #5(token budget + 智能压缩)是下一项,有可能会顺手把 cheap compress LLM 和 planner/verifier 复用主 LLMClient 的模式统一一下。
- 2026-05-11 · **Wave 3 #5 阶段 1** — `AppConfig` 加两个 schema 字段为后续 LLM 压缩做铺垫,纯数据无运行时影响。`src/openmimi/config/schema.py` 改动:① `from typing import Literal`;② `AppConfig` 新增 `max_context_tokens: int = 80000`(Sonnet 200K 上下文留 60% 余量,远大于现有 turn-based 限制);③ `AppConfig` 新增 `compression_strategy: Literal["truncate", "summarize"] = "truncate"` — Literal 在 pydantic 里会自动校验非法字符串,默认 `"truncate"` 完全保留旧行为(`_compress_old_tool_results` 不读这两个字段,后续 stage 才接);④ 加配套注释说明字段语义与默认值的意图。新增 3 个 `test_loop.py` 测试(放在 #7 stage 2 测试之后的新分隔区"Token-budget / compression (roadmap #5 stage 1)"下):默认值检查(`max_context_tokens==80000`, `compression_strategy=="truncate"`)、`pydantic.ValidationError` 触发(`compression_strategy="summary"` 拼写错误)、`"summarize"` 接受。**未动 `loop.py`**,这是 stage 3 的事。`pytest`: **229 passed** / 2 同样的 pre-existing failures(从 226 → 229,+3 个新测试)。Commit: `8f821bf feat(config): add max_context_tokens + compression_strategy (#5 stage 1)`。下一步:stage 2 实现 `compress_tool_result(text, llm, target_chars=500)` helper + token 估算函数,仍然纯库函数无 wiring,失败安全回落到 truncation。
- 2026-05-11 · **Wave 3 #5 阶段 2** — `compress_tool_result` + `estimate_tokens` 纯库函数落地。新建 `src/openmimi/compression.py`(~140 行,无 wiring,无 import 副作用 — 是 stage 3 才把它接进 loop):① `estimate_tokens(text) -> int` 用 `max(1, len // 4)` 启发式(避免分母零,empty → 0),挑这个常数是因为 Claude / GPT-4 / Qwen 在 ASCII + CJK 混合上平均都接近 4 chars/token,而且不引入 tokenizer 依赖;② `_TRUNCATED_SUFFIX` / `_COMPRESSED_SUFFIX` 两个标记,让 loop 后续能 grep 出是哪条路径生效;③ `_COMPRESS_SYSTEM` 常量 — 3 行 Did/Saw/Data 严格输出指令,"无 markdown / 无 prose / 无 fence";④ `_CompressLLM` Protocol 抽 `LLMClient.create` 用到的子集,这样测试无须造完整 LLMClient;⑤ `compress_tool_result(text, llm, *, target_chars=500, max_tokens=300, max_input_chars=8000)` 主函数:短文本原样返回 / `llm is None` 落到 `_truncate` / 长文本只取前 `max_input_chars` 送 LLM(防一条 100K tool_result 把 compression 自己也炸了)/ 任何异常或空 reply 落到截断 / 输出超过 `2 * target_chars` 再硬截一次(防 LLM 无视行数指令)/ 成功末尾加 `_COMPRESSED_SUFFIX` 让 loop grep 命中。**`compress_tool_result` 永不抛**。新增 10 个 `test_loop.py` 测试在"Token-budget / compression (roadmap #5 stage 2)"分隔区下:`estimate_tokens` 空 / 4-char 启发式 / 至少 1 三例、`compress_tool_result` 短文本不动 / llm=None 落截断 / 长文本调 LLM(检查 prompt 含 "tool output start") / LLM 抛异常落截断 / 空 reply 落截断 / 超长 summary 被 cap / 100K 输入只送 8K(prompt < 10K)。`pytest`: **239 passed** / 2 同样的 pre-existing failures(从 229 → 239,+10 个新测试)。下一步:stage 3 把 `_compress_old_tool_results` 改写成 `strategy="summarize"` 时调 `compress_tool_result(text, llm, target_chars=500)`,`strategy="truncate"` 时保留现有 `text[:400] + suffix` 行为 — 这是第一处真正动 `loop.py` 的 stage。
- 2026-05-11 · **Wave 3 #5 阶段 3** — `_compress_old_tool_results` 接 strategy + token 软上限。`src/openmimi/loop.py` 改动:① 顶部 `from typing import Literal` 与 `from .compression import compress_tool_result, estimate_tokens` 两条 import;② `sampling_loop` 加 3 个 kw-only 参数 `compression_strategy: Literal["truncate","summarize"] = "truncate"` / `max_context_tokens: int = 80000` / `compress_llm: LLMClient | None = None`(默认值 = 旧行为,所有现有 caller 0 改动);③ 函数体里调 `_compress_old_tool_results` 改成 `await ...` 并把上述 3 个值透传;④ `_compress_old_tool_results` 改成 `async def`,新增 4 个 kw `strategy / max_context_tokens / compress_llm / summarize_target_chars=500`,行为分两路:**truncate**(默认)= 旧 `text[:400] + _TRUNCATED_TEXT_SUFFIX` bit-for-bit 不变,**summarize** 路径先用 `_message_text(m)` 抽全消息文本算 `sum(estimate_tokens(...))`,如果 ≤ `max_context_tokens` 就 early-return 省钱(因为压缩本身要花 LLM token);超过软上限才走旧 turn-window,逐 text block 调 `compress_tool_result(text, compress_llm, target_chars=summarize_target_chars)`,LLM 失败时它自己会降级到截断;⑤ 新增 `_COMPRESSED_LLM_SUFFIX_FRAGMENT = "[compressed by LLM]"` 与 `_message_text(msg) -> str` 两个 module-level helper —— 后者把 str / list[block] 两种 content 形态统一成纯文本(text/tool_use json-dumps input/tool_result 拼内层 text),image 跳过,作为 `estimate_tokens` 的输入;⑥ 两条压缩路径都在 block 含 `_TRUNCATED_TEXT_SUFFIX` 或 `_COMPRESSED_LLM_SUFFIX_FRAGMENT` 时跳过,保证多次调用幂等不会重复压。新增 6 个 `tests/unit/test_loop.py` 测试在"_compress_old_tool_results strategy / budget gate (#5 stage 3)"分隔区下,带共享 `_msgs_with_old_long_tool_result` / `_extract_old_text` 两个 helper:truncate 路径 400 字符硬截 / summarize 在大 budget 下不动消息且不调 LLM / summarize 在小 budget 下调 LLM 一次并替换文本 / summarize 二次调用幂等 / summarize+`compress_llm=None` 降级到截断 / truncate 对已有截断后缀幂等。`pytest`: **245 passed** / 2 同样的 pre-existing failures(从 239 → 245,+6 个新测试)。Commit: `21c857a feat(loop): wire compression strategy + token budget gate (#5 stage 3)`。下一步:stage 4 把 `Orchestrator.from_env` 在 `cfg.compression_strategy=="summarize"` 时把主 `LLMClient` 透传成 `compress_llm`,沿用 #7 stage 3c 的"复用主 LLMClient"决策,然后 #5 整项收尾、焦点挪到 Wave 4 #9(grep 版 Memory v2)。
- 2026-05-11 · **Wave 3 #5 阶段 4 (#5 整项完结)** — Orchestrator 透传 compression 配置 + 复用主 LLMClient。`src/openmimi/orchestrator.py` 改动:① `Orchestrator.__init__` 加 `compress_llm: LLMClient | None = None` 可选 kw,存为 `self._compress_llm`(私有属性,仅供 sampling_loop 透传用);② `Orchestrator.from_env` 在所有 LLMClient 已构造完成之后插一行 `compress_llm = llm if cfg.compression_strategy == "summarize" else None`,reuse 主 LLMClient(complete 同 #7 stage 3c 的工程决策 — 不开第二条 cheap-model 通道,不增加 API key / 模型选择面),只在显式打开 summarize 时才打开 compress 注入;③ `run_task` 与 `run_chat_turn` 各自给 `sampling_loop(...)` 调用加 3 个 kw `compression_strategy=self.config.compression_strategy` / `max_context_tokens=self.config.max_context_tokens` / `compress_llm=self._compress_llm`(透传 AppConfig 的值,instance-stored compress_llm 由 `__init__` 或 `from_env` 决定)。**未改任何默认行为**:`AppConfig.compression_strategy` 仍默认 `"truncate"`,即所有现有用户启动 OpenMimi 后 `_compress_old_tool_results` 的行为完全不变(老的 400 字符硬截);需要用户在 `~/.openmimi/config.yaml` 里显式写 `compression_strategy: summarize` 才会切到智能压缩 + 软上限路径。新增 5 个 `tests/unit/test_orchestrator.py` 测试在"Compression strategy + token budget wiring (#5 stage 4)"分隔区下:`run_task` 透传 3 个 kw / `run_chat_turn` 透传 3 个 kw / 默认 truncate 路径 compress_llm=None / `from_env` summarize 时 reuse 主 LLM / `from_env` truncate 时 compress_llm 留 None。`pytest`: **250 passed** / 2 同样的 pre-existing failures(从 245 → 250,+5 个新测试)。**`#5` Token budget + 智能 LLM 压缩整项完结**(stage 1 schema + stage 2 纯库函数 + stage 3 loop wiring + stage 4 Orchestrator wiring)。下一步:Wave 4 #9 grep 版 Memory v2(Episodic JSONL + memory_grep/read/write/list 工具,**不引入向量数据库**)。
- 2026-05-11 · **Wave 4 #9 阶段 1** — Episodic 数据层落地。新建 `src/openmimi/memory/episodic.py`(~150 行,纯库函数,无 wiring):① 落盘格式 `data/memory/episodic/<YYYY-MM>/<session_id>.jsonl`,每行一个 step record;② `EpisodicStore(base_dir)` 三方法接口:`append_step(session_id, step, record, ts=None)` 自动注入 `ts/session_id/step` 三个 meta 键(以 meta args 为准,record 里同名键被覆盖)并按 ts 的月份决定写入哪个 `<YYYY-MM>/` 子目录;`read_session(session_id)` 跨月聚合所有 `<sid>.jsonl` 文件并按 step 升序排返回(单条坏 JSON 行 warning + skip,不 poison 整个会话读);`list_sessions(month=None, domain=None)` 列出 JSONL 路径,month=None 时遍历所有月份,domain 过滤通过 peek 第一条 record 的 `domain` 字段实现;③ `_safe_session_id` 用 `re.sub(r"[^A-Za-z0-9._-]", "_", ...)` 去除路径遍历字符(`../escape` → `.._escape`),空串归一为 `"session"`;④ `_MONTH_DIR_RE` 限制只遍历 `YYYY-MM` 模式的子目录,杂目录(如 `garbage/`)被忽略;⑤ `_first_record_domain` 只读首条 dict record 的 `domain` 字段,malformed / 缺 domain 都返回 None。`__init__.py` 导出 `EpisodicStore`。新建 `tests/unit/test_memory_episodic.py`(14 例):append 写一行带 auto-meta / append 多行追加 / meta 覆盖用户键 / read_session 按 step 排序 / 未知 session 返回 [] / 跳过 malformed 行 / 跨月份聚合 / list_sessions 不过滤全列 / list_sessions 按月过滤 / list_sessions 按 domain 过滤 / list_sessions 忽略非 month 目录 / `_safe_session_id` 净化测试 / append 路径遍历安全 / append 默认 ts 用 now()。`pytest`: **264 passed** / 2 同样的 pre-existing failures(从 250 → 264,+14 个新测试)。下一步:stage 2 把 `EpisodicStore` 接进 `loop.py`,每个 tool 调用之后追加一条 step record(record 形如 `{tool, action, result_summary, is_error, error_code, url, domain}`),与 `JsonlAuditLogger` 平行写但写到不同位置(audit 是逐 session 的运营日志,episodic 是跨 session 的可检索记忆)。
- 2026-05-11 · **Wave 4 #9 阶段 2** — `EpisodicStore` 接进 `loop.py` + `Orchestrator` 默认实例化。`src/openmimi/loop.py` 改动:① 顶部加 `from .memory.site_store import extract_domain`;② 新增 `EpisodicSink` Protocol(`append_step(*, session_id, step, record) -> Any`),与现有 `AuditSink` Protocol 同体例,`EpisodicStore` 结构性满足该 Protocol,测试可用 `_RecordingEpisodic` 假实现替身免文件系统依赖;③ `sampling_loop` 加 `episodic: EpisodicSink | None = None` kw-only 参数(默认 None,所有现有 caller 行为 100% 不变);④ 三个新 helper 模块函数:`_episodic_record(*, tool, tool_input, result_summary, is_error, error_code, url=None, domain=None)` 把 record 字段统一成同一 schema(action 从 `tool_input["action"]` 抽出,非 str 时为 None);`_result_url_and_domain(result)` 从 `ToolResult.details["url"]` 派生 URL+domain(navigate / get_url 等 handler 已经塞了 url),其它 handler 都返回 (None, None);`_safe_append_step(episodic, session_id=, step=, record=)` 调 `episodic.append_step(...)` 并用 `try/except` 吞所有异常(disk full / 权限错都不阻塞 in-flight tool call,只 warning log);⑤ 4 个 audit 落点(protocol error / timeout / exception / success)旁边各加一行 `_safe_append_step(...)` 调用,episodic gate 与 audit gate **独立**(`episodic is None` 和 `audit is None` 互不依赖)。`src/openmimi/orchestrator.py` 改动:① 顶部加 `from .memory.episodic import EpisodicStore`;② `Orchestrator.__init__` 加 `episodic: EpisodicStore | None = None` kwarg,存为 `self._episodic`;③ `Orchestrator.from_env` 在 `compress_llm` 决定之后插一行 `episodic = EpisodicStore()`(默认 base_dir = `data/memory/episodic`),再透传到 `cls(...)`;④ `run_task` 与 `run_chat_turn` 各自给 `sampling_loop(...)` 调用加 `episodic=self._episodic` kw 透传。新增 12 个测试 — `test_loop.py` 8 个(`_RecordingEpisodic` 假实现,共享 record 格式断言:成功调用写一条 / 从 `details["url"]` 派生 url+domain / `is_error=True` 透传 error_code / 异常 → episodic 仍写一条 is_error=True/error_code=TOOL_INTERNAL_ERROR / 空 tool_name → tool="<unknown>" / timeout → episodic 写 timed out / `OSError` 内部抛异常但 loop 不挂 / 默认 None 不写)+ `test_orchestrator.py` 4 个(`run_task` / `run_chat_turn` 透传 episodic / 默认 None / `from_env` 实例化 EpisodicStore)。`pytest`: **276 passed** / 2 同样的 pre-existing failures(从 264 → 276,+12 个新测试)。下一步:stage 3 实现 `memory_grep / memory_read / memory_write / memory_list` 4 个 `ToolBase` 子类并在 `Orchestrator` 里注册,完成 #9 主线;`memory_grep` 用 Python `re` 模块(不引 ripgrep 依赖)在 episodic 目录 + 可选 sites 目录里 grep。
- 2026-05-11 · **Wave 4 #9 阶段 3 (#9 主线完结)** — 4 个 LLM 可见的 memory 工具落地。新建 `src/openmimi/tools/memory.py`(~480 行,纯 stdlib,无 ripgrep / 无向量库依赖):① **`MemoryGrepTool`** —— 子串或正则搜索 `<root>/{episodic,sites,skills}`,输出 `ripgrep -n` 风格 `<scope>/<rel-path>:<line>:<text>`;参数 `pattern` 必填,`scope`(可选,默认搜三个)、`glob`(fnmatch)、`regex`、`ignore_case`、`max_matches`(默认 50)可选;每行截 200 字符,文件错误降级跳过且 warning 日志。② **`MemoryReadTool`** —— 路径必须以 scope 开头(`sites/github.com.md`),`offset`(1-based,默认 1)+ `limit`(默认 200 行,上限 1000)做窗口读;缺文件 → `TARGET_NOT_FOUND`;`details` 含 path/total_lines/offset/limit。③ **`MemoryWriteTool`** —— `episodic` scope 故意只读(系统管 JSONL,挡 LLM 误写破坏一行一 record 不变量),返 `PERMISSION_DENIED`;`sites/` 与 `skills/` 支持 `write` / `append` 两种 mode;500KB 单次上限。④ **`MemoryListTool`** —— `scope` 必填(三选一),`episodic` 支持 `month=YYYY-MM` + `domain=<host>` 过滤(`domain` 走首条 JSON 记录的 `domain` 字段,缺则不命中);non-episodic 给 `month/domain` 直接报错;`glob` 可选事后过滤;最多列 200 条。⑤ **路径遍历安全** —— 顶层 `_split_scope_and_rel(path)` 强制首段 ∈ {episodic, sites, skills}(Windows `\` 正规化),`_resolve_scope_path(root, scope, rel)` 用 `Path.resolve()` + `is_relative_to()` 双保险,任何 `../` 逃逸返 `INVALID_INPUT` 而非沉默写到任意位置。⑥ 全部 4 个工具 emit `structured` 字段(matches list / files list / path metadata)供 sub-agent / Planner 直接 JSON 消费,**不依赖 LLM parse output 字符串**(与 #12 ToolResult.structured 一致体例)。`src/openmimi/tools/__init__.py` 导出 4 个新类。`src/openmimi/orchestrator.py` 改动:`from_env` 在 `FileTool/CodeTool` 之后注册 4 个 memory tool(`MemoryGrepTool()` / `MemoryReadTool()` / `MemoryWriteTool()` / `MemoryListTool()`,全用默认 `memory_root="data/memory"`,与 `EpisodicStore` 的默认 `data/memory/episodic` 自然对齐)。新建 `tests/unit/test_memory_tools.py`(42 例):路径 split / resolve 安全(2 个 ✓ + Windows `\` 正规化 + 未知 scope + traversal 阻断 ✕2 + scope 不存在);grep(子串跨 scope / scope filter / regex / ignore_case / glob / no matches / max_matches 截断 / 空 pattern 报错 / 未知 scope / bad regex,10 例);read(content / offset+limit / missing / traversal / unknown scope / directory rejected / empty path,7 例);write(创建 / 嵌套子目录 / append 保留原内容 / episodic 拒写 / traversal / 仅给 scope 缺文件名 / 非法 mode / 超 500KB / 缺 content,9 例);list(sites / skills / episodic all / month filter / domain filter / unknown scope / non-episodic 滤错 / glob 过滤 / 空目录,9 例);所有 4 工具的 to_params schema 健康检查 + 名字唯一性(2 例)。`pytest tests/unit/test_memory_tools.py`: **42 passed in 1295s**(Windows pytest-asyncio 慢但稳)。`pytest tests/unit/test_orchestrator.py`: **28 passed**(无回归,从 22 个已存 + 0 新增因为这一步只在 `from_env` 加 4 行 `tools.register(...)`,既有 orchestrator 测试自动覆盖)。**`#9` Memory v2 主线完结**(stage 1 EpisodicStore 数据层 + stage 2 loop 接线 + stage 3 4 个 LLM 工具),可选 stage 4(`SiteMemoryStore` → markdown)与 stage 5(procedural skills 模板)留到后续。Commit 信息:`feat(tools): add memory_grep/read/write/list LLM tools (#9 stage 3)`。下一步:Wave 5 #8 Sub-agent 隔离。
- 2026-05-11 · **Wave 5 #8 阶段 1+2** — Sub-agent 数据层 + 运行器落地。新建 `src/openmimi/sub_agent.py`(~270 行,纯库,无 wiring):① `@dataclass SubAgentRequest(task, allowed_tools=None, max_turns=10, system_addendum="", parent_session_id="", sub_index=0)` —— 输入契约。`allowed_tools=None` 继承父级全集,`[]` 显式禁用所有工具,非空 list 只 keep 父级中存在的那几个(typos 静默跳过 + warning,不挂);② `@dataclass SubAgentResult(text, turns=0, sub_session_id="", is_error=False, error_message=None, messages=[])` —— 输出契约。`text` 是父 agent 唯一能看见的字段;成功 / 空 text / 异常三态都会填一段对父友好的 `text`;`messages` 留着给调用方调试 / Replay 用,父 LLM 不需要碰;③ `default_sub_session_id(parent, idx)` —— `<parent>--sub-<N>` 格式,parent 为空 / 空白时降级 `sub-<N>`,episodic JSONL 落盘按这条 id 路径独立隔离;④ `filter_tools(parent, allowed)` —— 返回 fresh `ToolCollection` 共享 `ToolBase` 实例(daemon 状态保留!),`None`/`[]`/list 三态语义清晰;不 mutate 父级;dedup;非 string 项跳过;⑤ `SubAgentRunner(llm, parent_tools, episodic=None, audit=None, max_tokens=4096, only_n_most_recent_images=2)` —— 主入口。`run(req) -> SubAgentResult`:builds fresh messages,filters tools,build sub-agent system prompt(focus-prompt + 可选 addendum),调 `sampling_loop(...)` 透传 episodic / audit / sub_session_id;空 task 短路返回 is_error;任何异常被 catch 转成 is_error result;`max_turns=0` 归一为 1(永远试一次);无 final text(loop 在 tool_use 上耗尽 turn)也算 is_error。⑥ `tools/collection.py` 顺手加 3 个 accessor:`names() -> list[str]` / `get(name) -> ToolBase | None` / `__contains__` —— sub-agent 用来 filter,Orchestrator/测试也省一次戳 `_tools` 私有字段。新建 `tests/unit/test_sub_agent.py`(36 例)+ 共享 `_ScriptedLLM`/`_NoopTool`/`_RecordingEpisodic` 假实现:`default_sub_session_id`(带 parent / 空 parent / 空白 parent)2 例;`filter_tools`(None=全 / [] = 空 / 子集 / 跳过 unknown / dedup / 共享 instance / 非 string 跳过 / 不 mutate parent)8 例;`ToolCollection` 新 accessor 1 例;`_build_sub_system_prompt`(无 addendum / 有 addendum / 空白 strip)3 例;`_final_assistant_text`(str / blocks / 跳过 tool_result / 无 text 空)4 例 + `_count_assistant_turns` 1 例;dataclass 默认值 2 例;`SubAgentRunner.run` 快乐路径(单 reply / one-tool-then-reply / addendum 进 system / allowed_tools 过滤 / [] 无工具 / None 全工具)6 例;失败路径(空 task / 空白 task / LLM 异常 / 无 final text / max_turns=0 归一)5 例;episodic 隔离(sub_session_id 用 sub--N 不复用父 / 没有 episodic 也工作)2 例;不 mutate parent / messages 隔离 2 例。`.venv/Scripts/pytest.exe tests/unit/test_sub_agent.py -q`: **36 passed in 0.10s**。全套 `pytest tests/`:**354 passed / 2 pre-existing failures**(从 318 → 354,+ 36 个新测试,2 个失败是 `OPENMIMI_DISABLE_SCREENSHOTS` blocker 老案,与本次无关)。**未做**:`SubAgentTool` ToolBase + Orchestrator `from_env` 注册留给 stage 3 —— 因为 Orchestrator 还得给 SubAgentTool 注入"当前 session_id 提供器"(run_task 每次产新 session_id,run_chat_turn 复用旧 session_id),这块要小心,单独一个 commit 更安全。下一步:阶段 3 把 runner 包成 LLM 可见工具并接到 Orchestrator,完成 #8。
- 2026-05-11 · **Wave 5 #8 阶段 3 (#8 整项完结)** — `SubAgentTool` LLM 工具化 + Orchestrator 接线。新建 `src/openmimi/tools/sub_agent_tool.py`(~190 行):① `SubAgentTool(name="sub_agent")` 持有一个 `SubAgentRunner` + 可选 `session_provider: Callable[[], str]` + `itertools.count()` 单调计数器,`to_params()` 暴露 schema `{task(必需 string), allowed_tools(可选 string array), max_turns(可选 int default 10), system_addendum(可选 string)}`;② `__call__` 输入校验:`task` 必须是非空非空白 string,`allowed_tools` 必须是 list(`[]` 显式禁用)且只 keep 字符串元素,`max_turns` 必须是 int(显式排除 `bool`,因 Python 里 `True is int` 是 True),`system_addendum` 非 string 时自动 `str(...)` 强转;③ 每次 `__call__` 调 `_current_parent_session_id()`(防御性 try/except,provider 抛异常或 None 时返回 ""),`_next_index()` 拿到全局递增 `sub_index`,构造 `SubAgentRequest(parent_session_id=..., sub_index=N)` 并 `await self._runner.run(req)`;④ 返回 `ToolResult(output=result.text, is_error=result.is_error, details={"sub_session_id":..., "turns":...}, structured={"turns":..., "sub_session_id":..., "is_error":..., 可选 "error_message":...})` —— `output` 给父 LLM 看,`structured` 给 Planner / 父监督逻辑直接 JSON 消费;⑤ `set_session_provider(callable)` 留作 late-bind 接口,因为 `SubAgentTool` 在 `from_env` 里得在 Orchestrator 实例存在**之前**就被注册到 tools(`SubAgentRunner` 需要 frozen parent toolset),没法在 `__init__` 时就把 `lambda: orch._current_session_id` 写死 —— 解法是构造完 `orch` 后再 `sub_agent_tool.set_session_provider(lambda: orch._current_session_id)`。**循环 import 修复**:首次把 `from .sub_agent_tool import SubAgentTool` 写进 `tools/__init__.py` 时炸了 `cannot import name 'SubAgentRequest' from partially initialized module 'openmimi.sub_agent'`,链路是 `tools/__init__.py` → `tools/sub_agent_tool.py` → `openmimi.sub_agent` → `loop` → `tools.collection` → 回到 `tools/__init__.py`。解决:`tools/__init__.py` **不**导出 `SubAgentTool`,加注释说明这个约束;`orchestrator.py` 改成 `from .tools.sub_agent_tool import SubAgentTool` 直接拉子模块。**Orchestrator 改动**:① `__init__` 新增 `self._current_session_id: str = ""` 状态;② `from_env` 在 `episodic = EpisodicStore()` 之后:`SubAgentRunner(llm=llm, parent_tools=tools, episodic=episodic)` → `SubAgentTool(sub_agent_runner)` → `tools.register(sub_agent_tool)` → 构造 `orch = cls(...)` → `sub_agent_tool.set_session_provider(lambda: orch._current_session_id)`;③ `run_task` 第一行后插 `self._current_session_id = session_id`,`run_chat_turn` 开头插同样语句 —— 保证 SubAgentTool 任何一次 `__call__` 都能拿到当时的 session id 而不是构造时刻的空字符串。新增 27 个测试 —— `tests/unit/test_sub_agent_tool.py` 23 例(`_FakeRunner` 假 runner:`to_params` schema 健康 / 转发请求到 runner / 可选字段透传 / `allowed_tools` 过滤非字符串静默 / `[]` 保留 / 计数器单调 / `session_provider` 每次重读 / `None` provider 返回空 / provider 抛异常返回空 / `set_session_provider` rebind / `task` 缺失或空或空白或非字符串各自 INVALID_INPUT / `allowed_tools` 非 list INVALID_INPUT / `max_turns` 非 int INVALID_INPUT / `max_turns=True` bool 排除 / `max_turns=0` 透传 / `system_addendum` 非 string 强转 / runner 失败的 is_error/error_message 传到 ToolResult / 成功时 structured 不含 error_message / 工具名是 "sub_agent" / schema required 仅 task) + `tests/unit/test_orchestrator.py` 4 例(`run_task` 设置 `_current_session_id` / `run_chat_turn` 设置 / `from_env` 注册 `sub_agent` 工具 / `from_env` provider lambda 读得到 orch 的当前 session)。`pytest tests/unit/test_sub_agent_tool.py`: **23 passed**(快路径)。`pytest tests/unit/test_orchestrator.py`: **32 passed in 908.89s**(Windows pytest-asyncio 慢但稳)。Commit: `f556275 feat(orchestrator): register sub_agent tool with session-id provider (#8 stage 3)`。**`#8` Sub-agent 隔离整项完结**(stage 1+2 数据层+运行器 + stage 3 工具化 + Orchestrator 接线)。下一步:Wave 5 #11 Replay 脚本(纯 stdlib HTML 渲染,把 episodic JSONL + audit JSONL 拼成单页可视化)。

- 2026-05-11 · **Wave 5 #11 阶段 1** — Replay 纯库渲染器。新建 `src/openmimi/replay.py`(~190 行,纯 stdlib `html`/`json`):`build_html(records, *, title=None) -> str` 接受 `EpisodicStore.read_session()` 形状的 dict 序列,返回一份完整的 HTML5 单页(`<!doctype html>` + 内联 `<style>` + 一组 `<details>`,每个 step 一个 collapsible 块);① 头部 `<h1>Session <sid></h1>` 含会话 id,`.meta` 行 auto 拼 `"<N> step(s)" · "<M> errors" · "<first_ts> → <last_ts>"`(单 timestamp / 同 timestamp 时省略箭头,0 errors 省略 error 段);② 每个 step 一个 `<details>`,`<summary>` 含 `#<step>` step 编号 + tool 名(粗体)+ action(蓝色)+ `ok`(绿)或 `ERROR <code>`(红,粗体,is-error 类整块红底)+ url(灰色)五段;`<details>` body 优先渲染 `result_summary` 为 `<pre>` 块,然后把所有其他键(如 `ts`/`domain`/`sub_session_id`/`duration_ms`)dump 成 JSON 进第二个 `<pre>`;③ XSS 安全 —— 所有用户控制的字段(tool/action/url/result_summary/title/error_code)都过 `html.escape`,JSON dump 整段过 escape;`<script>alert(1)</script>` 被渲染成 `&lt;script&gt;` 字面量;④ 容错 —— 非 dict 项静默跳过(支持 None / 字符串混进 list 的脏数据);缺 `step` 渲染为 `#?`;缺 `tool` 渲染为 `<unknown>`;空 records 渲染 `<p class="empty">No step records in this session.</p>`;⑤ 接受 `Iterable[Mapping]` 而不是 `list[dict]`,generator 也能直接传入;`title` 优先 explicit 参数 > 首条记录的 `session_id` > 兜底 `"OpenMimi replay"`。新建 `tests/unit/test_replay.py`(27 例):基本 shape(`<!doctype>` / `<head>` / `<body>` / 空记录提示 / 三档 title 兜底)5 + step 渲染(`<details>` 数对 / step number / action 显示 / 错误标记 / ok 标记 / url 渲染 / pre 块 / unknown keys → JSON / 非 dict 跳过)9 + XSS 转义(tool/result/url/action/title 五处)5 + meta 行(单/复数 step / error 计数 / 时间范围 / 单 timestamp 不显箭头)5 + 缺字段(step ?/ tool unknown / generator 输入)3。初次测试有 1 个失败 —— `test_no_error_marker_when_ok` 断言 "is-error" 不出现,但 CSS 定义里有 `.is-error {...}` 字面命中,改成断言 `class="is-error"`(HTML 属性形式)修复。`.venv/Scripts/python.exe -m pytest tests/unit/test_replay.py -q`: **27 passed in 0.04s**。**未做**:CLI 入口(`python -m openmimi.replay`)、audit log 合并(`tool_input`/`image_path`/`duration_ms`)、screenshot 渲染。这三块在 stage 2 一起做,因为它们要碰文件系统 + argparse,会让 stage 1 的纯函数测试变重。Commit: `ec8e1f7 feat(replay): add build_html pure renderer for session replay (#11 stage 1)`。下一步:stage 2 加 CLI + merge episodic/audit + image_path → `<img>` 渲染。

## Blockers

- `tests/unit/test_cli.py::test_run_no_screenshots_sets_env` 和 `tests/unit/test_env_flags.py::test_screenshots_disabled_default`:测试断言的 env 变量名(`OPENMIMI_DISABLE_SCREENSHOTS`)与实现读取的(`OPENMIMI_ENABLE_SCREENSHOTS`)语义反转。**预存在的失败**,与 #1 无关,可在 Wave 1 顺手修。

## 延期项(未来可能要做)

(暂无)
