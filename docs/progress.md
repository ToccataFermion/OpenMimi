# OpenMimi 迭代开发进度

> 起始:2026-05-11。按 `docs/improvement_roadmap.md` 中"四、推荐执行顺序"推进。
> cron 每 6 分钟唤醒一次,读这个文件,继续下一项,完成后更新这里。

## 当前焦点

**Wave 3 #7 阶段 3c (待开始)**:Orchestrator 接线 + `enable_planning` gate。阶段 3a (`LLMVerifier`) 与 3b (`LLMPlanner`) 已落地;现在把它们串到 `Orchestrator` 上:① `Orchestrator.__init__` 接受可选 `planner: LLMPlanner | None = None, verifier: Verifier | None = None`(未传时根据 `AppConfig.enable_planning` 选择 — `True` 时如果 LLM 客户端可用就构造 LLMPlanner+LLMVerifier,`False` 时保持 None);② `run_task` / `run_chat_turn` 在 `enable_planning=True` 时先 `await planner.plan_task(task, system_context)` 拿 Plan,把 plan 摘要(`step 1: ... [criteria: ...]` 一行一条)拼进 system prompt 第一帧,然后 `sampling_loop(..., plan=plan, verifier=verifier)` 把两者透传给 loop;③ 失败安全 — Planner 永远返回单步 Plan,所以即使 planning 路径出错最坏也只是 NullVerifier + 单步 Plan;④ 测试:Orchestrator 注入 ScriptedLLM 的双客户端(主 + planner/verifier),验证 plan 摘要进 system prompt + sampling_loop 收到 plan 与 verifier;⑤ 完成后 `#7` 整项 [x],焦点挪到 Wave 3 #5(token budget + 智能压缩)。**注**:`AppConfig` 可能需要再加一个 `planning_model: str | None`(同 provider 选不同 model)— 留到 3c 看 LLMClient 接口怎么注入再决定。

## Wave 1 — 清债 + 基础(本周)

- [x] #1 删除 legacy `browser.py`,从 `tools/__init__.py` 摘掉 `BrowserTool` 导出
- [x] #2 修复 / 移除 skills 加载(`skills.py` 指向已删除目录)
- [x] #3 错误码扩容 + 结构化恢复提示(20 个 + `next_step_hint` + `make_error_result` helper)
- [x] #6 Daemon prewarm(REPL 启动时即触发 `_ensure_daemon`)

## Wave 2 — 工程解耦(下周)

- [x] #4-lite `agent_browser.py` dispatcher 化(拆 `actions/` 子目录,主类只做路由 + daemon 管理)
  - [x] step 1 — `actions/` 包 + registry + `navigation.py`(navigate/back/forward/reload)
  - [x] step 2 — interaction 族(click/right_click/double_click/check/uncheck/type/fill/react_fill/press/key_combo/hover)
  - [x] step 3 — scroll 族(scroll/human_scroll/scroll_until/scroll_into_view)
  - [x] step 4 — extract / page-state 族(snapshot/extract/page_source/get_url/get_title/get_attribute/set_attribute/get_property/get_box/is_visible/visual_locate)
  - [x] step 5 — wait 族(wait/wait_for/wait_for_disappear/wait_for_navigation/wait_for_network_idle)
  - [x] step 6 — tab/session 族(tab_*/save_session/load_session/clipboard/clear_cache)
  - [x] step 7 — network/cdp/storage/pdf/console/screenshot 族
  - [x] step 8 — 剩余杂项(eval/batch/drag/mouse/focus/select/upload/download/emulate_device/set_*)
  - [x] step 9 — 主类只剩 `__init__` / `__call__` / `_dispatch` / daemon 管理(`_TOOL_DESCRIPTION` + `to_params` schema 抽出到 `agent_browser_schema.py`)
- [x] #12 结构化 ToolResult(新增可选 `structured: dict` 字段)

## Wave 3 — 智能跃迁(2-3 周)

- [ ] #7 Planner / Executor / Verifier 三角
  - [x] 阶段 1 — `Plan`/`PlanStep`/`Verifier` 数据结构 + Protocol + `NullVerifier`(默认关闭,`AppConfig.enable_planning=False`)
  - [x] 阶段 2 — 在 `loop.py` 里接入 Verifier 做 turn-end 自评(返回 done / continue / replan)
  - [x] 阶段 3a — `LLMVerifier`(用 LLMClient 给 success_criteria 打分,失败安全降级"continue")
  - [x] 阶段 3b — `LLMPlanner`(任务分解 → `[{step, success_criteria, allowed_tools?, budget?}]`,失败时单步 plan)
  - [ ] 阶段 3c — Orchestrator 接线 + `enable_planning` gate + system prompt 注入
- [ ] #5 Token budget + 智能 LLM 压缩(替代 400 字符截断)

## Wave 4 — 跨 session 记忆(3-4 周)

- [ ] #9 Memory v2(grep 版):episodic / semantic / procedural 三层 + `memory_grep/read/write/list` 工具

## Wave 5 — 隔离 + 调试

- [ ] #8 Sub-agent 隔离(`spawn(task, tools_subset, max_turns) → result`)
- [ ] #11 轻量 Replay 脚本(`python -m openmimi.replay <session_id>` → 单页 HTML)

## 工作守则(给未来唤醒的我)

1. 每次唤醒先 `git status` + 读这个文件,确认从哪里继续。
2. 一次只做一小步(目标:每次唤醒能产出 1 个 commit,或在长项目中推进一个明确小阶段)。
3. 完成后必须:
   - 跑现有测试 `pytest tests/` 确认没破坏
   - 创建 git commit(信息清晰、作用域窄)
   - 在本文件上方标 `[x]`,把"当前焦点"指到下一项
4. 不能自主决策的问题(API/架构方向不明、需要外部账号等)→ 写入 `docs/blockers.md`,跳过该项继续下一个。
5. 不要扩张范围:遇到看似相关但不在已选项内的事项,记到本文件末尾的"延期项"下,不要现场改。
6. 失败/破坏性操作前(`rm -rf` / `git reset --hard` / 强推)→ 一律先停手记录到 blockers,等用户回来。

## 已完成

- 2026-05-11 · **Wave 1 #1** — 删除 `browser.py` (1108 行)、`browser_schema.py` (344 行)、相关测试与脚本;`tools/__init__.py` 收敛导出。`pytest`: 62 passed / 2 pre-existing failures(与本改动无关,见 Blockers)。
- 2026-05-11 · **Wave 1 #2** — 删除 `src/openmimi/skills.py`(指向已删除目录的死代码);**顺手修复**:`Orchestrator._build_system_prompt` 之前完全没注入 site memory(写但不读),现在通过 `memory.format_for_prompt(domain)` 真正接到 system prompt 上。`pytest`: 仍 62 passed / 2 同样的 pre-existing failures。
- 2026-05-11 · **Wave 1 #3** — `tools/errors.py` 从 5 个 ErrorCode 扩到 20 个,覆盖 element / network / auth / page-state / execution / system 六类;每个 code 配一条 `next_step_hint` 结构化恢复提示;新增 `make_error_result(code, message, ...)` helper,把提示同时写进 `output`(LLM 可见)和 `details`(审计);新增 `tests/unit/test_errors.py`(7 例,覆盖 hint 完整性 / StrEnum 兼容 / extra_details 合并)。`pytest`: 69 passed / 2 同样的 pre-existing failures。**注**:Wave 1 #4 之前不大改 `agent_browser.py`,所以暂未把现有错误路径迁到新 helper(留待 dispatcher 化时一起做)。
- 2026-05-11 · **Wave 1 #6** — Daemon prewarm 显式化。背景:`AgentBrowserTool.__init__` 已经在后台线程跑 `_start_warmup`,但用户感知不到首条任务为什么慢。本次改动:① `AgentBrowserTool.is_warming_up()` 公开接口;② `Orchestrator` 持有 `browser_engine` 引用,新增 `prewarm_browser() -> bool`(失败安全);③ `cli.chat()` 与 `cli.chat_main()` 在 welcome 之后调用 `_announce_prewarm(orch)`,有 warmup 在飞时打一行 `browser : warming up in background (first task may be slow)`。新增 `tests/unit/test_prewarm.py`(7 例,覆盖无 engine / 飞 / 完成 / 异常 / 三种 announce 路径)。`pytest`: 76 passed / 2 同样的 pre-existing failures。**注**:没有真正"挪"启动时间(它本来就在后台跑),而是把它**可见化** — 这是用最小改动达成 roadmap 意图的方式,等 Wave 1 #4 dispatcher 化之后如果还需要可以再做 sync await。
- 2026-05-11 · **Wave 2 #4-lite step 1** — `agent_browser.py`(3766 行)dispatcher 化第 1 步:① 新建 `tools/actions/` 包 + 注册表(`@register(name)` 装饰器、`get(name)`、`registered_actions()`);② 新建 `actions/navigation.py`,把 `navigate/back/forward/reload` 4 个 handler 从 `_do_*` bound method 迁出为接受 `engine` 参数的自由函数;③ `_dispatch` 在查询内置 dict 之前先咨询 registry,registry 命中即返回;④ 删除 `agent_browser.py` 里这 4 个 method 与对应的 dispatch 项(-40 行)。增量、向后兼容(主类外观完全不变)、不需要再回炉 — 后续 step 复用同一脚手架。新增 `tests/unit/test_actions_registry.py`(6 例,覆盖注册表查询 / 4 个 handler 与 fake engine 的协作)。`pytest`: 82 passed / 2 同样的 pre-existing failures。`agent_browser.py` 现 3726 行,目标是 < 1000 行。
- 2026-05-11 · **Wave 2 #4-lite step 2** — interaction 族 11 个 handler 外迁:① 新建 `actions/_keys.py`(verbatim 搬 `_cdp_key_code` + `_KEY_MAP`,留作 `actions/` 包内部 helper);② 新建 `actions/interaction.py`,迁 `click/right_click/double_click/check/uncheck/type/fill/react_fill/press/key_combo/hover` 11 个 handler + `_click_with_mouse` 私有 helper(自由函数版),全部用 `@register("name")` 装饰;③ `actions/__init__.py` 加 `from . import interaction` side-effect 注册;④ `agent_browser.py` 删 11 个 `_do_*` 方法 + `_click_with_mouse` 方法 + 4 份重复的 `_cdp_key_code` 顶层函数(原本只有 1 份,Edit 操作中放大了一次,清理时一并 verbatim-replace_all 删干净)+ dispatch 表里 11 个对应项。**净变化**: -407 行,`agent_browser.py` 从 3726 → 3319 行。新增 5 个新测试(交互族注册成员检查 + click/type/press/right_click 4 个 handler 行为冒烟,假 engine 走 `get box → mouse move/down/up` 三步序列)。`pytest`: 88 passed / 2 同样的 pre-existing failures。
- 2026-05-11 · **Wave 2 #4-lite step 3** — scroll 族 4 个 handler 外迁:① 新建 `actions/scroll.py`,迁 `scroll/human_scroll/scroll_until/scroll_into_view`;`scroll_until` 内部依赖 `engine._parse_data` / `engine._parse_snapshot`,`scroll_into_view` 走 `eval` 注入 JS,`human_scroll` 用 `random` + `asyncio.sleep` 模拟人;② `actions/__init__.py` 加 `from . import scroll` side-effect 注册;③ `agent_browser.py` 删 4 个 `_do_*` 方法 + dispatch 表里 4 个对应项。本次 Edit 中途因为 new_string 写错把方法块复制了一份(从而临时变成 3464 行),靠后续 `replace_all=true` 把 4-method 块 + 后随的 `_do_page_source` 行作为唯一 anchor 一次性删 1 份(只删了 1 份,因为另一份的 anchor 行不一致),再用 `replace_all=false` 精确删剩余 1 份,最终回到 3165 行(净 -154)。新增 5 个测试:scroll 族注册成员、`scroll` 子命令转发、`scroll_until` 首探就找到立即返回、`scroll_until` 缺参数报错、`scroll_into_view` 走 `eval`。`pytest`: 93 passed / 2 同样的 pre-existing failures。`agent_browser.py` 现 3165 行。
- 2026-05-11 · **Wave 2 #4-lite step 4** — extract / page-state 族 11 个 handler 外迁:① 新建 `actions/extract.py`(~530 行),迁 `snapshot/page_source/get_url/get_title/get_attribute/set_attribute/get_property/extract/get_box/is_visible/visual_locate`;`snapshot` 内嵌 CAPTCHA 检测路径,失败时返回带 `error_code: ErrorCode.CAPTCHA_DETECTED` 的非错误 ToolResult,提示 LLM 看截图解题;`get_attribute` / `set_attribute` / `get_property` 共享 ref → CSS-selector / target_text → treeWalker 的元素定位 JS 模板;`extract` 按 instruction 分支(get text / headings / links / forms / tables / metadata / images / 兜底)各自构造 JS,输出截到 4000 字符;`visual_locate` 用 OpenCV 模板匹配,临时把 `engine._screenshot_scale` 设 1.0 拿全分辨率截图后在 `finally` 还原,可选 click 走 mouse move/down/up;② `actions/__init__.py` 加 `from . import extract` side-effect 注册;③ `agent_browser.py` 删 11 个 `_do_*` 方法 + dispatch 表里 11 个对应项,3 批 Edit(snapshot+extract / get_box+is_visible+visual_locate / page_source+get_url+get_title+get_attribute+set_attribute+get_property)。**净变化**:-431 行,`agent_browser.py` 从 3165 → 2734 行,registry 现含 30 个 action(4 nav + 11 interaction + 4 scroll + 11 extract)。新增 7 个测试:extract 族注册成员检查 + `get_url`/`get_title`/`get_box` 子命令路由 + `get_box` 缺 selector 报错 + `extract` "get text" 截 4000 字符 + `get_attribute` 缺 attribute_name 报错 + `is_visible` 走 eval 含 getBoundingClientRect。`pytest`: 101 passed / 2 同样的 pre-existing failures。
- 2026-05-11 · **Wave 2 #4-lite step 5** — wait 族 5 个 handler 外迁:① 新建 `actions/wait.py`(~258 行),迁 `wait/wait_for/wait_for_disappear/wait_for_navigation/wait_for_network_idle`;`wait` 单纯转发 daemon 的 `wait` 子命令,其余 4 个都在 Python 端轮询(`time.monotonic` + `asyncio.sleep`)以避免占用 sidecar;`wait_for_navigation` 起步先抓一次当前 URL,然后轮询 `eval window.location.href` 直到变化(可选 `expected_url` 子串过滤);`wait_for_network_idle` 一次性向页面注入 `window.__openmimi_network_idle_*` fetch/XHR 拦截器(再注入幂等),然后轮询 in-flight 计数与 last-active 时间戳;② `actions/__init__.py` 加 `from . import wait` side-effect 注册;③ `agent_browser.py` 删 5 个 `_do_wait*` 方法(3 批 Edit:`_do_wait` 单独删 + 中途多删了 `async def _do_eval` 头部,立即用一次反向 Edit 修复 / `_do_wait_for`+`_do_wait_for_disappear` 合块删 / `_do_wait_for_navigation`+`_do_wait_for_network_idle` 合块删)+ dispatch 表 3 批共 5 项删除。**净变化**:-219 行,`agent_browser.py` 从 2734 → 2515 行,registry 现含 35 个 action。新增 7 个测试:wait 族注册成员检查 + `wait` 子命令转发 + `wait_for` 首探到立即返回 + `wait_for` 缺 target 报错 + `wait_for_disappear` 首探无 box 立即返回 + `wait_for_navigation` URL 变化检测 + `wait_for_network_idle` 注入 + idle 立即返回。`pytest`: 108 passed / 2 同样的 pre-existing failures。
- 2026-05-11 · **Wave 2 #4-lite step 6** — tab/session 族 8 个 handler 外迁:① 新建 `actions/tab_session.py`(305 行),迁 `clipboard/tab_list/tab_switch/tab_new/tab_close/clear_cache/save_session/load_session`;`tab_*` 4 个 handler 都先 `engine._refresh_tabs()` 再读 `engine._tabs` / 写 `engine._active_tab_index` 保持 sidecar 与 Python 镜像一致;`save_session` 优先用 CDP `Network.getAllCookies`(可拿 HTTP-only),失败回落 `document.cookie`;`load_session` 同时支持 CDP cookie 数组(`{name,value,domain,path}`)与 legacy 分号串两种存盘格式;② `actions/__init__.py` 加 `from . import tab_session` side-effect 注册;③ `agent_browser.py` 删 8 个 `_do_*` 方法(3 批:clipboard+tab_list+tab_switch+tab_new+tab_close 一块 / clear_cache 一块 / save_session+load_session 一块)+ dispatch 表 3 批共 8 项删除。**净变化**:-258 行,`agent_browser.py` 从 2515 → 2257 行,registry 现含 43 个 action。新增 8 个测试:tab_session 族注册成员检查 + `clipboard read/write` 转发 + `tab_list` 列表 + `tab_new` 子命令 + `save_session`/`load_session` 缺 file_path 报错 + `clear_cache` 走 eval 含 `localStorage.clear()`。`pytest`: 116 passed / 2 同样的 pre-existing failures。
- 2026-05-11 · **Wave 2 #4-lite step 7** — network/cdp/storage/pdf/console/screenshot 族 7 个 handler + 1 helper 外迁:① 新建 `actions/network_cdp.py`(708 行),迁 `cdp/screenshot/network_log/network_modify/storage/pdf/console`;`cdp` 通过 `window.__openmimi_cdp_send(method, params)` JS bridge 转发任意 CDP 方法,作为逃生口;`screenshot` 仍走 `engine._take_screenshot(path_override, annotate)`,但本族独立 import 了 `screenshots_disabled`(`agent_browser.py` 仍保留 import 因为 `_take_screenshot_raw` 也要用);`network_log` 注入幂等 `window.__openmimi_network_log` 拦截 fetch / XHR(再注入幂等),再读最多 20 条捕获;`network_modify` 5 个子动作(user_agent / inject_headers / block_urls / mock_response / clear),user_agent 优先走 CDP `Network.setUserAgentOverride` 失败回落 `Object.defineProperty`;`storage` 三类(localStorage / sessionStorage / cookies),cookies 优先走 CDP `Network.getAllCookies` / `Network.setCookie`(经 `_try_cdp_then_fallback` 自由函数 helper 串起来),失败回落 `document.cookie`;`pdf` 走 CDP `Page.printToPDF`,失败回落 `window.print()` 提示;`console` 注入 `window.__openmimi_console_logs`(max 200)shim,读最近 30 条按 level 过滤;② `_try_cdp_then_fallback` 从 method 改为自由函数(首参 engine),storage handler 内部更新调用;③ `actions/__init__.py` 加 `from . import network_cdp` side-effect 注册;④ `agent_browser.py` 删 7 个 `_do_*` 方法 + 1 个 `_try_cdp_then_fallback` helper(4 批 Edit:cdp+screenshot 一块 / network_log 单独 / network_modify 单独 / storage+helper+pdf+console 一块)+ dispatch 表 1 批共 7 项删除。**净变化**:-669 行,`agent_browser.py` 从 2257 → 1588 行,registry 现含 50 个 action。新增 14 个测试:network_cdp 族注册成员检查 + `cdp` value 拆封 + 缺 cdp_method 报错 + `screenshot` 启用/禁用两路 + `network_log` 安装+读 + `network_modify` user_agent / inject_headers 缺 headers + `storage` localStorage set / 缺 key / cookies CDP-first + `pdf` 缺 file_path / 成功 + `console` 安装+读。`pytest`: 130 passed / 2 同样的 pre-existing failures。`agent_browser.py` 现 1588 行,距目标 < 1000 还差 ~588 行,1-2 步可达。
- 2026-05-11 · **Wave 2 #4-lite step 8** — 杂项 13 个 handler 外迁(最后一族!):① 新建 `actions/misc.py`(~470 行),迁 `select/upload/download/eval/batch/drag/mouse/focus/set_viewport/emulate_device/set_timezone/set_locale/set_geolocation`;`select/upload/download` 是 sidecar passthrough(`engine._exec("select", ...)` 等);`eval` 用 `engine._parse_json` 拆封 success/data/result 三层结构,空结果给提示;`batch` 把 steps 数组直接拼到 `engine._exec("batch", "--bail", "--json", ...)`;`drag` 支持 ref+to_ref 或 target_text+to_target_text;`mouse` 4 个子动作(move/down/up/wheel)直接转 sidecar;`focus` Win32 EnumWindows + 评分(title 3 / domain 2 / url 2 / chromium 1),`urlparse` 改为 module-level import;`set_viewport` 走 `eval` 注入 `window.resizeTo()`;`emulate_device` 把 `_DEVICE_PRESETS` 提为模块常量,先试 CDP `Emulation.setDeviceMetricsOverride`(+ `setUserAgentOverride` + 可选 `setTouchEmulationEnabled`),失败回落 JS-only(`window.resizeTo` + `Object.defineProperty navigator.userAgent`);`set_timezone/set_locale/set_geolocation` 都是单条 CDP `Emulation.set*Override` eval 调用,`set_geolocation` 在缺 lat/lon 时调 `clearGeolocationOverride`;② `actions/__init__.py` 加 `from . import misc` side-effect 注册;③ `agent_browser.py` 删 13 个 `_do_*` 方法 + dispatch 表彻底清空(`_dispatch` 现在只保留 registry 查询,miss 即返回 `Unknown action: ...`,3 批 Edit:select+upload+download / emulate_device+set_timezone+set_locale+set_geolocation / eval+batch+drag+mouse+focus+set_viewport)。**净变化**:-414 行,`agent_browser.py` 从 1588 → 1174 行,registry 现含 63 个 action(全部 action handler 已外迁,只剩特殊路径 `_do_close`,因为它在 `__call__` 中走前置短路而非 `_dispatch`)。新增 16 个测试:misc 族注册成员检查 + select/upload 缺参数 + download 缺 selector + eval 空 js / 成功序列化 + batch 缺 steps + drag 缺配对目标 + mouse move 转发 + set_viewport 缺尺寸 / 成功 eval + emulate_device CDP 路径 + set_timezone 含 timezoneId + set_locale 含 locale + set_geolocation 清除 / 设置。`pytest`: 146 passed / 2 同样的 pre-existing failures。`agent_browser.py` 现 1174 行,距目标 < 1000 还差 ~174 行,step 9 (主类瘦身) 可达。
- 2026-05-11 · **Wave 2 #4-lite step 9 (#4-lite 完结)** — `_TOOL_DESCRIPTION` 长字符串(76 行)+ `to_params()` JSON Schema(330 行,含 64 项 action enum + 59 个 property 描述)抽出到新模块 `tools/agent_browser_schema.py`(431 行,纯数据,无 import 依赖 instance state);`agent_browser.py` 改为 `from .agent_browser_schema import TOOL_DESCRIPTION as _TOOL_DESCRIPTION, build_input_schema as _build_input_schema`,`to_params()` 缩为 6 行 thin wrapper(只组装 `{name, description, input_schema}` 三键 dict);`build_input_schema()` 每次 fresh 返回新 dict 避免共享可变状态。**净变化**:-401 行,`agent_browser.py` 从 1174 → 773 行(从起步 3766 行降了 -2993 行 / -79.5%,远低于 < 1000 行目标);schema 模块化后,如果未来需要加新 action,只需改一个地方而不是两个。**未抽出**:daemon 管理方法(`_start_browser` / `_exec` / `_take_screenshot` / `_parse_*` / `_inject_stealth` / `_refresh_tabs` / `_take_screenshot_raw` / `_browser_env` / `_start_warmup` / `is_warming_up` / `_detect_captcha`)与 `_do_close` 都依赖 instance state(`self._daemon_proc`、`self._tabs`、`self._screenshot_scale` 等),抽出会引入大量 engine 参数注入,得不偿失,留在主类。**未新增测试**:schema 抽出是纯文本搬迁,运行时行为不变,现有 146 个测试已经覆盖 `to_params()` 间接路径(每次 `Tool.to_params()` 调用都会触发 `_build_input_schema()`)。`pytest`: 146 passed / 2 同样的 pre-existing failures。**`#4-lite` 已完结**:从 3766 行 god class 拆成 1174 行(step 8) → 773 行(step 9)主类 + 8 个 actions/ 子模块(navigation/interaction/scroll/extract/wait/tab_session/network_cdp/misc)+ 1 个 agent_browser_schema.py,共 63 个 action handler 全部走 `@register` 注册表分发,主类只剩 daemon 管理 + 路由 + 一个特殊 close 短路。
- 2026-05-11 · **Wave 2 #12** — 结构化 `ToolResult.structured` 字段(可选 `dict[str, Any]`,默认 `None`)。设计要点:`output: str` 仍是 LLM 可读文本,`details: dict` 仍是元数据(error_code / next_step_hint),新增的 `structured` 是**数据载荷本身**(即 handler 内部已经构造好的 dict / list,免得上层再 re-parse `output` 字符串)。在 `tools/result.py` `@dataclass ToolResult` 加字段并写明语义注释。挑 4 个高价值 handler 做示范:① `extract`(get text / 6 个 instruction 分支)— `structured={"instruction": ..., "data": result_value}`;② `get_box` — `structured={"box": box, "selector": selector}`(与现有 `details` 同步);③ `is_visible` — `structured=result_value`(复用 result_value 的 visible/tag/rect dict);④ `network_log` — `structured={"requests": requests}`(20 条最近抓到的请求列表)。loop.py 不动 — `structured` 是给 Wave 3 #7 的 Planner 与 Wave 5 #8 的 Sub-agent 用的,LLM 端继续读 `output`。新增 6 个测试:`structured` 默认 None + 上述 4 个 handler 各自的 structured 内容断言 + extract links 列表场景。`pytest`: 152 passed / 2 同样的 pre-existing failures(从 146 → 152,+ 6 个新测试)。**Wave 2 完结**(#4-lite + #12 都已 [x])。
- 2026-05-11 · **Wave 3 #7 阶段 1** — Planner/Verifier 脚手架。新建 `src/openmimi/planning.py`(132 行,纯数据 + Protocol):`@dataclass PlanStep(step, success_criteria, allowed_tools=None, budget=None, done=False)` + `to_dict/from_dict`;`@dataclass Plan(steps, current_step=0)` + `current()`/`advance()`/`is_complete()`/`to_dict/from_dict`;`@runtime_checkable Verifier` Protocol(单方法 `async verify(plan, recent_messages) -> Literal["done","continue","replan"]`);`NullVerifier`(永远返回 `"continue"`)作为安全默认。`AppConfig` 加 `enable_planning: bool = False` 开关 — 阶段 1 完全无运行时影响,只是为阶段 2 准备好"骨架接得上"。**没动 `loop.py`**,这是阶段 2 的事。新增 `tests/unit/test_planning.py`(12 例:dataclass 默认值 + roundtrip + advance/current/is_complete 三态 + 空 Plan / 越界 advance 安全 + NullVerifier 三种入参路径 + Protocol isinstance 检查 + AppConfig 默认值)。`pytest`: 164 passed / 2 同样的 pre-existing failures(从 152 → 164,+12 个新测试)。下一步:阶段 2 把 Verifier 接进 `loop.py` 做 turn-end 自评。
- 2026-05-11 · **Wave 3 #7 阶段 2** — Verifier 接进 `loop.py`。`sampling_loop` 加 `verifier: Verifier | None = None, plan: Plan | None = None` 两个 keyword-only 参数(都默认 None,所有现有 caller 行为 100% 不变)。在每 turn 末尾(`messages.append({"role": "user", "content": tool_result_blocks})` + `_trim_old_images` + `_compress_old_tool_results` 之后),如果 `verifier and plan and not plan.is_complete()`,就 `await verifier.verify(plan, messages)`;outcome:`done` → `return messages` 提前结束;`replan` → 打 `[planner] verifier requested replan (stage 3 hook)` stderr 标记继续(阶段 3 在这里挂 Planner);`continue` 或任何意外值 → 无操作。Verifier 抛异常被 try/except 吞掉(降级 `continue`)— 一个坏 verifier 不会拖垮整个会话。`Orchestrator` 不动,因为新参数都默认 None。新增 7 个 `test_loop.py` 测试 + 一个 `_RecordingVerifier` 假实现:no verifier 跳过 / 有 verifier 但 plan=None 跳过 / NullVerifier 不影响循环 / done outcome 早退(只跑 1 turn 而不是 3 turn) / replan 不影响 / verifier 异常被吞 / 完成 plan 跳过。`pytest`: 171 passed / 2 同样的 pre-existing failures(从 164 → 171,+7 个新测试)。下一步:阶段 3 接入 Planner LLM(任务分解 → 子目标列表)+ `LLMVerifier`(用便宜模型给 success_criteria 打分),把 #7 整项收尾。
- 2026-05-11 · **Wave 3 #7 阶段 3a** — `LLMVerifier` 落地。`src/openmimi/planning.py` 在 `NullVerifier` 之后新增:① `_LLM_VERIFIER_SYSTEM` 常量(strict-JSON 系统提示,要求 `{"outcome":"done|continue|replan","reason":"..."}` 无 prose/markdown/fences);② `_OUTCOMES` 元组用于枚举校验;③ `_extract_response_text(response)` 从 Anthropic 响应里抽文本块(兼容 `content: str | list[block]`);④ `_parse_outcome(text)` — 先直接 `json.loads(text.strip())`,失败回退 `re.search(r"\{.*\}", text, re.DOTALL)` 抓首个 `{...}`,然后校验 `outcome ∈ _OUTCOMES`,无效返回 None;⑤ `_summarize_messages(messages, max_chars_per_block=400)` 把 Anthropic 消息渲染成 `[role/type] payload` 一行一条(text / tool_use / tool_result(含 ERROR 标记) / image 各自分支),每块截 400 字符给廉价模型友好的小 prompt;⑥ `LLMVerifier(llm, *, max_tail_messages=6, max_tokens=200, system_prompt=None)` — `verify(plan, msgs)` 中 `plan.current() is None` 直接返回 `"done"`(免 LLM 调用),否则 tail-clip + 构造 user prompt(含 step / success_criteria / 活动摘要 / 末尾"Reply with ONLY"指令)调 `llm.create(system, messages, tools=[], max_tokens)`,**任何异常**(网络 / API / JSON / 枚举无效)都降级到 `"continue"` — 一个坏 verifier 永远不能拖垮一个能跑的会话。`__all__` 加上 `LLMVerifier`。新增 20 个测试:`_parse_outcome` 5 个 valid(普通 / 紧凑 / replan / 前置 prose / fenced JSON)+ 6 个 invalid(空 / 无 json / outcome 值非法 / 缺 outcome key / 烂 JSON / 裸字符串)、`_summarize_messages` 2 个(综合 + ERROR 标记)、`LLMVerifier` 7 个(空 plan 不调 LLM / done passthrough / continue+replan 连环 / 烂 JSON 降级 continue / 异常降级 continue / tail-clip 只保留末 N 条 / Protocol isinstance)。`pytest`: **191 passed** / 2 同样的 pre-existing failures(从 171 → 191,+20 个新测试)。Commit: `30f417f feat(planning): add LLMVerifier (#7 stage 3a)`。**注**:`LLMVerifier` 持有任意 `LLMClient`(调用方挑模型 — Orchestrator 阶段 3c 会接一个 Haiku-class 廉价模型),所以测试不需要造任何真实 LLM provider;`_ScriptedLLM` stub 单类够用。下一步:阶段 3b 实现 `LLMPlanner`,同样的"失败永不阻塞"策略 + `_ScriptedLLM` 复用。
- 2026-05-11 · **Wave 3 #7 阶段 3b** — `LLMPlanner` 落地。`src/openmimi/planning.py` 在 `LLMVerifier` 之后新增:① `_LLM_PLANNER_SYSTEM` 常量(strict-JSON 系统提示,要求 top-level array,每项 `{step, success_criteria, allowed_tools?, budget?}`,1-5 步 / 复杂 ~8 步);② `_parse_plan_steps(text)` — 先直接 `json.loads`,失败回退 `re.search(r"\[.*\]", text, re.DOTALL)` 抓首个 `[...]`,逐项校验 — `step` 与 `success_criteria` 必须是非空字符串(strip 后),`allowed_tools` 若是列表则只留非空字符串项(空列表归一为 None),`budget` 必须是正 int(显式排除 `bool` — Python 里 `True is int`),malformed 项跳过;若一项都没活下来返回 None;③ `_single_step_plan(task)` — 兜底 Plan,把 task 包成单步 `step=task, success_criteria=f"task accomplished: {task}"`,空 task 换成"complete the user's request";④ `LLMPlanner(llm, *, max_tokens=1024, system_prompt=None)` — `plan_task(task, system_context="")` 中空 task 直接返回 `_single_step_plan` 不调 LLM;否则构造 user prompt(含可选 `Environment context: ...` 段 + `User task: ...` + 末尾"Reply with ONLY"示例)调 `llm.create(...)`,**任何异常 / 烂 reply / 空 array** 都降级到 `_single_step_plan(clean_task)` — `plan_task` 永远返回可用 Plan,never None never raises。`__all__` 加上 `LLMPlanner`。新增 24 个测试:`_parse_plan_steps` 7 个 valid(最小 / 可选字段 / strip / 噪声前后 / 跳过 malformed / 过滤非字符串 tools / 过滤无效 budget)+ 7 个 invalid(空 / 无 json / 空 array / 全 malformed / dict 非 array / 烂 json / 裸字符串)、`_single_step_plan` 2 个(普通 / 空 task)、`LLMPlanner` 8 个(happy path / task+context 进 prompt / 空 context 不出现 "Environment context" / 空 task 不调 LLM / 烂 reply 降级 / 空 array 降级 / 异常降级 / 部分 malformed 项被丢)。`pytest`: **215 passed** / 2 同样的 pre-existing failures(从 191 → 215,+24 个新测试)。Commit: `d4cdf88 feat(planning): add LLMPlanner (#7 stage 3b)`。下一步:阶段 3c 把 LLMPlanner + LLMVerifier 接到 `Orchestrator`,在 `enable_planning=True` 时先 `await planner.plan_task(...)` → 注入 system prompt + 把 plan/verifier 透传给 `sampling_loop`。

## Blockers

- `tests/unit/test_cli.py::test_run_no_screenshots_sets_env` 和 `tests/unit/test_env_flags.py::test_screenshots_disabled_default`:测试断言的 env 变量名(`OPENMIMI_DISABLE_SCREENSHOTS`)与实现读取的(`OPENMIMI_ENABLE_SCREENSHOTS`)语义反转。**预存在的失败**,与 #1 无关,可在 Wave 1 顺手修。

## 延期项(未来可能要做)

(暂无)
